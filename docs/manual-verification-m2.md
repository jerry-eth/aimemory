# M2 人工验证清单

自动化已覆盖:Release 构建(0 错误)、当前全量 171 个测试(169 通过、2 个真实 LLM 端点测试因未配置 `AMM_TEST_LLM_KEY` 跳过)、启动冒烟、仓库无密钥泄漏检查。
以下项涉及真实桌面交互 / 真实大模型服务 / 通知与视觉效果,无法无头验证,请人工走查。

## 1. 大模型页 — DeepSeek 档案(BYOK)
- [x] 打开「大模型」设置页 → 新增档案:名称 DeepSeek、Base URL `https://api.deepseek.com/v1`、粘贴自己的 API Key、保存(2026-08-20 前已人工完成,档案存在且密钥为 DPAPI 密文)
- [x] 点「测试连接」→ 显示连接成功(失败时给出可读错误信息,不崩溃)(2026-08-20 自动化:连接成功,发现 2 个模型)
- [x] 点「拉取模型」→ 下拉框列出真实模型列表,选择模型并保存(2026-08-20 自动化:列表 2 项,选择保存后 llm-profiles.json 同步更新)
- [x] 点「设为当前」→ 该档案标记为当前使用,重启应用后仍为当前(2026-08-20 自动化:settings.json ActiveProfileId 正确,重启后「当前使用」标记仍在)

## 2. 大模型页 — Ollama 档案(本地,无密钥)
- [x] 新增档案:Base URL `http://localhost:11434/v1`、模型名任意(如 `qwen2.5:7b`)、密钥留空 → **可正常保存**(本地地址不再要求密钥)(2026-08-20 自动化通过)
- [x] 同样的空密钥 + 远程地址(如 `https://api.deepseek.com/v1`)→ 保存被拦,提示"远程服务必须填写 API 密钥"(2026-08-20 自动化通过,提示与档案未落盘均验证)
- [x] 本地 Ollama 运行时:测试连接成功、拉取模型能列出本地模型(2026-08-20 自动化:启动 ollama serve 后连接成功,发现 4 个本地模型)
- [x] Ollama 未运行时:测试连接给出明确失败提示,不崩溃(2026-08-20 自动化:提示「连接失败:由于目标计算机积极拒绝,无法连接。(localhost:11434)」)

## 3. 智能分析页 — 分析与缓存
- [x] 打开「智能分析」页 → 点「开始分析」→ 等待后出现建议卡片(进程名、动作、理由、风险)(2026-08-20 自动化:报告卡+建议出现,消耗 509+363 Token)
- [x] 在进程/模板未变化的情况下再点一次「开始分析」→ 结果显示「(缓存结果)」,且 Token 统计不新增消耗(2026-08-20 自动化:显示「本次消耗 0 + 0 Token (缓存结果…)」,jsonl 行数不变)
- [x] 出现建议后点「强制刷新」→ 跳过缓存重新请求,Token 统计新增一条消耗(2026-08-20 自动化通过)
- [x] 改变系统进程状态(新开大程序)后再分析 → 触发真实请求而非缓存(2026-08-20 自动化:新起 200MB 标记进程后缓存失效)
- [x] 修改「自定义附加指令」或切换语言后再分析 → 缓存失效,触发真实请求(2026-08-20 自动化:改自定义指令后缓存失效)

> 自动化说明:活跃开发机上 Top30 进程 60 秒内桶位/成员必然变化(实测 6/30),缓存永不可能命中;
> 测试期间将 Top40 活跃进程临时加白名单制造安静快照集,并用 200MB 常驻标记进程保证建议非空。
> 期间还发现并修复:快照哈希按内存降序排列,同名进程顺序互换导致真实系统上缓存几乎永不命中
> ——已改为按 (名称,桶) 稳定排序,含回归单测。

## 4. 压缩建议执行
- [x] 建议卡片中对可压缩进程点「执行压缩」→ 完成提示 → 回仪表盘确认内存占用下降(2026-08-20 自动化:msedge 工作集 451MB→315MB,「已释放」提示出现;注:页外卡片 UIA 不报滚动,测试用窗口最大化+鼠标滚轮露出按钮)

## 5. 提示词模板
- [x] 提示词设置中编辑默认模板(如要求理由用英文、或改变语气)→ 重新分析 → 建议理由的语言/风格随之变化(2026-08-20 自动化:整模板替换为固定输出指令后,建议理由变为模板指定内容;注:实测 DeepSeek 对「reason 加前缀」类软指令不稳定遵守,硬覆盖指令稳定遵守)
- [x] 模板中的 `{memory_info}` `{process_list}` `{custom_instructions}` `{language}` 四个变量在发送前被替换为实际内容(单元测试 AnalysisPromptBuilderTests 覆盖占位替换;自动化模板中亦含四变量)
- [x] 点「恢复出厂」→ 模板内容回到内置默认,分析结果恢复原有风格(2026-08-20 自动化:恢复后 prompts.json 与内置默认一致)

## 6. 自动触发
- [x] 开启阈值触发,把阈值调到容易达到的值 → 让内存占用跑高 → 自动分析完成(2026-08-20 自动化:阈值 40%+4.3GB 内存压力至 45%,调度器 tick 后 jsonl 新增 Trigger=1 阈值记录)
- [x] 当日重复触发达到每日上限后 → 不再自动分析(2026-08-20 自动化:上限=1,加压 150s 无新增;注:本机撤压后占用未回落到阈值以下,触发位未重置,E2E 的"不再触发"由触发位+上限共同保证,纯上限拦截由单测「达每日上限后不再阈值触发」确定性覆盖;验证后已恢复原设置)

## 7. Token 统计
- [x] 「Token 统计」页数字与数据目录下 jsonl 记录逐条一致(输入/输出 token 数)(2026-08-20 自动化:本月聚合卡与 jsonl 聚合完全一致 48,774+46,447/26 次,最近调用首行时间吻合)
- [x] 在档案中填单价 → 统计页显示估算费用(2026-08-20 自动化:单价 2 → 显示 $0.1904,与 jsonl 计算一致;验证后已恢复 0)
- [x] 把月度预算写一个很小的 Token 数(如 100)→ 触发自动分析时被预算闸门拦停,统计页/通知有体现(2026-08-20:预算告警 InfoBar 正常显示,截图 artifacts/tokenstats-budget.png;注:WPF-UI InfoBar 不进 UIA 自动化树,无法自动断言,已截图人工确认;闸门逻辑有单测覆盖)
- [x] 预算用满后在「智能分析」页手动点「开始分析」→ 被拦,状态栏提示已达月度预算(2026-08-20 自动化:StatusText 显示「已达月度预算,自动分析已暂停」,jsonl 不新增;验证后预算已恢复 0)

## 8. 内存泄漏告警
- [x] 用测试程序制造持续增长 → 观察窗口内收到泄漏告警(2026-08-20 自动化:窗口 5min/阈值 50MB,uismokeleaker 每 10s 长 15MB 且页粒度重触保持 WS,~5 分钟告警行出现;Windows 通知弹窗本身未做 UIA 断言)
- [x] 泄漏告警行右侧点「智能分析」→ 以泄漏触发器发起分析,结果正常展示,Token 统计中该条触发方式为「泄漏」(2026-08-20 自动化:jsonl 新增 Trigger=3)
- [x] 「大模型」页「泄漏检测」卡片:开关、增长阈值(50-10000 MB)、观察窗口(5-120 分钟)改动即保存,重启后保持(2026-08-20 自动化:50→80/5→10 经 UI 改动,settings.json 同步,重启后 UI 保持,验证后已恢复)

> 自动化期间发现并修复两个真实问题:
> 1. 泄漏告警发生后「智能分析」页的告警行不会实时刷新(原仅在 VM 构造和分析完成时刷新)——已改为订阅 LeakDetected 即时刷新。
> 2. 进程工作集被任何外部清理(含本应用的定时规则)裁剪都会因「回落重置观察窗」推迟告警,属预期行为;测试期间需关闭清理规则。

## 9. 密钥存储检查
- [x] 用文本编辑器打开数据目录下 `llm-profiles.json` → API Key 字段为 DPAPI 密文,**无明文密钥**(2026-08-20 检查:EncryptedApiKey 为 DPAPI Base64 密文,文件内无任何明文密钥)
- [ ] 在别的用户账户/机器上拷贝该文件 → 无法解密出原密钥(可选)
