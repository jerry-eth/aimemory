# M3 完善版实现计划 — AI 内存管家

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补齐产品完整形态:L3 结束进程(确认流+未保存检测+后悔药)、清理历史、防误杀名单、开机自启/通知开关/全局热键、C 盘瘦身助手(扫描→LLM 建议→回收站删除/Junction 迁移)。

**Architecture:** 沿用 M1/M2 服务层模式:一切 Win32 调用收敛在 `INativeMemoryApi` 后(扩展两个方法);L3 终止由独立 `ProcessTerminateService` 执行,白名单/系统关键/防误杀/前台四重防护在执行端强制重查(不信 UI);C 盘瘦身复用 M2 LLM 管线(新提示词模板族 + 新解析器),删除一律走回收站(Microsoft.VisualBasic.FileIO),迁移用 robocopy 复制+校验+mklink /J+日志回退。

**Tech Stack:** .NET 8、WPF-UI 4.3.0、新增包:System.Management(WMI 取命令行,FR-2.7)、Xunit.SkippableFact(技术债)。已有:CommunityToolkit.Mvvm、System.Security.Cryptography.ProtectedData。

## Global Constraints

- 目标框架 `net8.0-windows`;配置/数据文件一律在 `%APPDATA%\AiMemoryManager\`;dotnet 命令一律 `"/c/Program Files/dotnet/dotnet.exe"`
- **L3 默认必须用户确认**;内置系统关键名单永不进 L3 候选;白名单/防误杀名单内进程 L3 永不自动执行;大模型 L3 建议必须过滤后才展示
- **执行端强制重查**:ProcessTerminateService 对 each pid 在终止前重新校验(系统关键/白名单/防误杀/前台),不依赖 UI 过滤
- **C 盘瘦身边界**:只做分析与建议+用户确认后的执行;删除一律入回收站(不用永久删除);系统路径硬白名单(C:\Windows、C:\Program Files*、C:\ProgramData 核心)永不进建议;迁移前检测进程占用
- FR-12.1 的"Windows Update 缓存"被 FR-12.5 硬白名单覆盖,不纳入候选(安全规则优先)
- 真实密钥严禁入库(集成测试走环境变量);表单明文密钥只存内存
- 全部界面文案 i18n:新 key 双语同步(zh-CN.json + en.json),不得改已有 key
- XAML:禁止 `Fill="None"`(用 `{x:Null}`);WPF-UI 4.3.0 标准 Page;ThemeResource 枚举缺成员用 `{DynamicResource}`
- 服务事件多触发于线程池,UI 订阅方必须封送 Dispatcher;async-void 处理器必须 try/catch
- commit 信息用中文;测试项目已有 FakeNativeMemoryApi 可复用(扩展接口时同步更新)

## 已有接口(直接消费,不得改签名)

- `INativeMemoryApi`:GetSystemMemory / GetProcessSnapshots / EmptyWorkingSets / GetForegroundPid / IsFullscreenAppActive
- `CleanService`:`RunL1Async(CleanTrigger, ct)` / `RunL2Async(CleanTrigger, ct)` → `CleanResult(Time, Level, FreedBytes, ProcessCount, Trigger)`;`event EventHandler<CleanResult> CleanCompleted`
- `CleanLevel { L1, L2 }`、`CleanTrigger { Manual, RuleThreshold, RuleTimer, Tray }`(本计划扩展)
- `WhitelistService`:`IsExcluded(name)` / `IsSystemCritical(name)` / `Add/Remove/Import/Export`;`Excluded` IReadOnlyCollection
- `ForegroundGuard.IsProtected(pid)`;`SettingsService`(Current/Save/SettingsSaved);`LocalizationService`(`this[key]`/CurrentLanguage)
- M2:`Locator.Analysis.AnalyzeAsync(AnalysisTrigger, forceRefresh=false, ct)`、`AnalysisResult/AnalysisSuggestion(ProcessName, Action, Reason, Risk)`、其余 Locator 服务
- `Locator`:静态服务定位器,M3 新增服务挂这里

## AppSettings 新增字段(Task 1 加入)

```csharp
public List<string> NoKillProcesses { get; set; } = new();   // FR-7.2 防误杀名单
public bool AutoStartEnabled { get; set; } = false;          // FR-8.3
public bool NotificationsEnabled { get; set; } = true;       // FR-8.4
public int HotkeyModifiers { get; set; } = 0x0002 | 0x0004;  // FR-8.5: MOD_CONTROL|MOD_SHIFT
public int HotkeyKey { get; set; } = 0x4D;                   // 'M'
```

## 枚举扩展(Task 1)

```csharp
public enum CleanLevel { L1, L2, L3 }
public enum CleanTrigger { Manual, RuleThreshold, RuleTimer, Tray, Analysis }
```

## INativeMemoryApi 扩展(Task 1)

```csharp
bool TryTerminateProcess(int pid, out int win32Error);   // OpenProcess(PROCESS_TERMINATE)→TerminateProcess→CloseHandle;失败带 GetLastWin32Error
IReadOnlyList<string> GetWindowTitles(int pid);          // EnumWindows 过滤归属 pid 的可见顶层窗口标题(含无标题返回空串)
```

## File Structure(新增)

```
src/AiMemoryManager/
  Models/CleanHistoryEntry.cs      清理历史条目
  Models/KillRecord.cs             后悔药记录
  Models/TerminateResult.cs        TerminateItemResult / TerminateResult
  Models/DiskScan.cs               FolderSizeInfo / DiskCandidate / DiskCategory
  Models/DiskAdvice.cs             DiskCleanableItem / DiskMigratableItem / DiskAdvice
  Models/MigrationLogEntry.cs
  Services/AtomicFile.cs           原子写助手(tmp+move),技术债统一
  Services/CleanHistoryService.cs  FR-2.5
  Services/ProcessTerminateService.cs  FR-2.3 执行端
  Services/UnsavedStateDetector.cs FR-2.6
  Services/KillLogService.cs       FR-2.7
  Services/StartupService.cs       FR-8.3
  Services/HotkeyService.cs        FR-8.5
  Services/DiskScanService.cs      FR-12.1
  Services/DiskCandidateProvider.cs FR-12.1 候选清单
  Services/DiskAdviceService.cs    FR-12.2 + SystemPathGuard(FR-12.5)
  Services/RecycleBinDeleteService.cs FR-12.3
  Services/MigrationService.cs     FR-12.4 + 占用检测(FR-12.5)
  ViewModels/ProcessesViewModel.cs     扩展(L3 勾选/确认流/后悔药)
  ViewModels/CSlimViewModel.cs     新建
  Views/CSlimPage.xaml(.cs)        新建
  Views/TerminateConfirmDialog.xaml(.cs) 新建(L3 确认对话框,进程页与分析页共用)
  Controls/ 无新增
tests/AiMemoryManager.Tests/
  CleanHistoryServiceTests / ProcessTerminateServiceTests / UnsavedStateDetectorTests
  KillLogServiceTests / WhitelistNoKillTests / StartupServiceTests(可测部分)
  DiskScanServiceTests / DiskAdviceParserTests / SystemPathGuardTests
  RecycleBinDeleteServiceTests / MigrationServiceTests / 其余随任务
```

## 关键接口(后续任务依赖,签名不得改)

```csharp
// Models/CleanHistoryEntry.cs
public record CleanHistoryEntry(DateTimeOffset Time, CleanLevel Level, long FreedBytes, int ProcessCount, CleanTrigger Trigger);

// Services/CleanHistoryService.cs — %APPDATA%\AiMemoryManager\clean-history.json,容量100
public class CleanHistoryService
{
    public const int Capacity = 100;
    public CleanHistoryService(string filePath);
    public IReadOnlyList<CleanHistoryEntry> Entries { get; }   // 新在前
    public event EventHandler? Changed;
    public void Record(CleanHistoryEntry entry);               // 头插+截断+落盘(原子写)
    public static string DefaultPath();
}

// Models/KillRecord.cs
public record KillRecord(DateTimeOffset Time, int Pid, string Name, string? Path, string? Arguments);

// Services/KillLogService.cs — %APPDATA%\AiMemoryManager\kill-log.json,容量20
public class KillLogService
{
    public const int Capacity = 20;
    public KillLogService(string filePath, Func<int, string?>? commandLineProvider = null, Action<string, string?>? starter = null);
    public IReadOnlyList<KillRecord> Records { get; }          // 新在前
    public void Record(KillRecord record);                     // 头插+截断+落盘;Path/Arguments 由 commandLineProvider 补全
    public bool Restart(KillRecord record);                    // starter(Path, Arguments);Path 为空/文件不存在→false
    public static string DefaultPath();
}

// Models/TerminateResult.cs
public record TerminateItemResult(int Pid, string Name, bool Success, int Win32Error);
public record TerminateResult(long FreedBytes, IReadOnlyList<TerminateItemResult> Items);

// Services/UnsavedStateDetector.cs — FR-2.6 启发式
public class UnsavedStateDetector
{
    public UnsavedStateDetector(INativeMemoryApi native);
    public bool HasUnsavedSigns(int pid);   // 任一可见窗口标题含 '*' 或 '•'
}

// Services/ProcessTerminateService.cs — 执行端强制重查
public class ProcessTerminateService
{
    public ProcessTerminateService(INativeMemoryApi native, WhitelistService whitelist,
        ForegroundGuard guard, UnsavedStateDetector detector, KillLogService killLog);
    public IReadOnlyList<int> FilterCandidates(IReadOnlyCollection<int> pids);  // 去掉系统关键/白名单/防误杀/前台
    public Task<TerminateResult> TerminateAsync(IReadOnlyCollection<int> pids); // 内部先 FilterCandidates 再逐个 TryTerminateProcess,成功记 KillLog
    public event EventHandler<TerminateResult>? TerminateCompleted;
}

// WhitelistService 扩展(FR-7.2,与 Excluded 同款 volatile 快照模式)
public IReadOnlyCollection<string> NoKill { get; }
public bool IsNoKill(string processName);
public void AddNoKill(string processName);
public void RemoveNoKill(string processName);

// Services/StartupService.cs — FR-8.3(MSIX StartupTask 优先,未打包回退注册表 Run 键)
public class StartupService
{
    public StartupService(SettingsService settings);
    public bool IsEnabled { get; }           // 读实际系统状态(非仅设置)
    public void SetEnabled(bool enabled);    // 同步系统状态+写设置;失败不抛,记 Debug
}

// Services/HotkeyService.cs — FR-8.5
public class HotkeyService : IDisposable
{
    public event EventHandler? Pressed;
    public bool Register(IntPtr hwnd, int modifiers, int key);  // RegisterHotKey;已注册先 Unregister
    public void Unregister();
    public IntPtr WndProc(IntPtr hwnd, int msg, IntPtr wParam, IntPtr lParam, ref bool handled); // HwndSource.AddHook 用,WM_HOTKEY→Pressed
    public void Dispose();
}

// Models/DiskScan.cs
public enum DiskCategory { Temp, BrowserCache, RecycleBin, UserFolder, Other }
public record FolderSizeInfo(string Path, long SizeBytes, int FileCount, DiskCategory Category);
public record DiskCandidate(string Path, DiskCategory Category);

// Services/DiskScanService.cs
public class DiskScanService
{
    public Task<FolderSizeInfo> MeasureAsync(string path, DiskCategory category, CancellationToken ct = default); // 迭代遍历,访问拒绝/超长路径跳过
    public Task<IReadOnlyList<FolderSizeInfo>> ScanAsync(IReadOnlyList<DiskCandidate> candidates, CancellationToken ct = default);
}

// Services/DiskCandidateProvider.cs — FR-12.1 候选(不扫 C:\Windows)
public static class DiskCandidateProvider
{
    public static IReadOnlyList<DiskCandidate> GetCandidates();
    // %TEMP%、Chrome/Edge 缓存、回收站(C:\$Recycle.Bin,访问失败跳过)、%USERPROFILE% 下一级目录按大小 Top15
}

// Services/SystemPathGuard.cs — FR-12.5 硬白名单
public static class SystemPathGuard
{
    public static bool IsProtected(string path);
    // 保护:C:\Windows、C:\Program Files、C:\Program Files (x86)、C:\ProgramData(及其子路径),大小写不敏感,盘符取系统驱动器
}

// Models/DiskAdvice.cs
public record DiskCleanableItem(string Path, string Reason, long EstBytes);
public record DiskMigratableItem(string Path, string Reason, string TargetDrive);
public record DiskAdvice(IReadOnlyList<DiskCleanableItem> Cleanable, IReadOnlyList<DiskMigratableItem> Migratable);

// Services/DiskAdviceService.cs — FR-12.2,复用 M2 LLM 管线
public class DiskAdviceService
{
    public DiskAdviceService(LlmProfileService profiles, ILlmClient client, LocalizationService l10n,
        TokenStatsService stats, IReadOnlyList<string> availableFixedDrives);
    public Task<DiskAdvice> AnalyzeAsync(IReadOnlyList<FolderSizeInfo> scan, CancellationToken ct = default);
    // 提示词内嵌常量(磁盘专用,不进 PromptTemplateService);输出经 SystemPathGuard + 候选集双重过滤;Token 记 AnalysisTrigger.Manual
    public event EventHandler<DiskAdvice>? AdviceCompleted;
}

// Services/RecycleBinDeleteService.cs — FR-12.3
public class RecycleBinDeleteService
{
    public bool DeleteDirectoryToRecycleBin(string path);  // Microsoft.VisualBasic.FileIO.FileSystem,UIOption.OnlyErrorDialogs
    public bool DeleteFileToRecycleBin(string path);
}

// Models/MigrationLogEntry.cs
public record MigrationLogEntry(DateTimeOffset Time, string Source, string Target, string Junction, bool Reverted);

// Services/MigrationService.cs — FR-12.4,日志 %APPDATA%\AiMemoryManager\migration-log.json(容量50)
public class MigrationService
{
    public MigrationService(INativeMemoryApi native, string logPath);
    public IReadOnlyList<MigrationLogEntry> Log { get; }
    public IReadOnlyList<string> GetBlockingProcesses(string folderPath);   // 进程路径前缀匹配,FR-12.5
    public Task<MigrationLogEntry> MigrateAsync(string source, string targetRoot, CancellationToken ct = default);
    // robocopy /E 复制→文件数校验→删除源(永久,对话框已明确警告)→mklink /J→记日志;任一步失败尽量回滚现场并抛 InvalidOperationException
    public Task<bool> RevertAsync(MigrationLogEntry entry);  // 删 Junction→robocopy 移回→更新日志 Reverted=true
    public static string DefaultLogPath();
}
```

---

### Task 1: 枚举/接口扩展 + CleanHistoryService + 原子写助手(TDD)

**Files:**
- Create: `src/AiMemoryManager/Services/AtomicFile.cs`, `src/AiMemoryManager/Models/CleanHistoryEntry.cs`, `src/AiMemoryManager/Services/CleanHistoryService.cs`
- Modify: `src/AiMemoryManager/Models/CleanResult.cs`(枚举扩展), `src/AiMemoryManager/Native/INativeMemoryApi.cs`(+2 方法), `src/AiMemoryManager/Native/NativeMemoryApi.cs`(实现), `src/AiMemoryManager/Models/AppSettings.cs`(+5 字段), `tests/AiMemoryManager.Tests/Fakes/FakeNativeMemoryApi.cs`(实现 2 新方法)
- Test: `tests/AiMemoryManager.Tests/CleanHistoryServiceTests.cs`, `tests/AiMemoryManager.Tests/AtomicFileTests.cs`

**Interfaces:**
- Produces: 头部全部"关键接口"中 AtomicFile/CleanHistoryService/枚举/INativeMemoryApi 扩展/AppSettings 字段。

- [ ] **Step 1: 写失败测试**

`AtomicFileTests.cs`:
```csharp
using AiMemoryManager.Services;

namespace AiMemoryManager.Tests;

public class AtomicFileTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "amm-test-" + Guid.NewGuid().ToString("N"));
    public AtomicFileTests() => Directory.CreateDirectory(_dir);
    public void Dispose() => Directory.Delete(_dir, true);

    [Fact] public void 写入后内容正确且不留tmp文件()
    {
        var p = Path.Combine(_dir, "a.json");
        AtomicFile.WriteAllText(p, "{\"x\":1}");
        Assert.Equal("{\"x\":1}", File.ReadAllText(p));
        Assert.False(File.Exists(p + ".tmp"));
    }

    [Fact] public void 覆盖已存在文件()
    {
        var p = Path.Combine(_dir, "a.json");
        File.WriteAllText(p, "old");
        AtomicFile.WriteAllText(p, "new");
        Assert.Equal("new", File.ReadAllText(p));
    }

    [Fact] public void 目录不存在时自动创建()
    {
        var p = Path.Combine(_dir, "sub", "a.json");
        AtomicFile.WriteAllText(p, "x");
        Assert.True(File.Exists(p));
    }
}
```

`CleanHistoryServiceTests.cs`:
```csharp
using AiMemoryManager.Models;
using AiMemoryManager.Services;

namespace AiMemoryManager.Tests;

public class CleanHistoryServiceTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "amm-test-" + Guid.NewGuid().ToString("N"));
    private readonly string _path;
    public CleanHistoryServiceTests()
    {
        Directory.CreateDirectory(_dir);
        _path = Path.Combine(_dir, "h.json");
    }
    public void Dispose() => Directory.Delete(_dir, true);

    private static CleanHistoryEntry E(int mb, CleanTrigger t = CleanTrigger.Manual) =>
        new(DateTimeOffset.Now, CleanLevel.L1, (long)mb << 20, 3, t);

    [Fact] public void 记录后新在前且触发Changed事件()
    {
        var s = new CleanHistoryService(_path);
        int fired = 0;
        s.Changed += (_, _) => fired++;
        s.Record(E(10));
        s.Record(E(20));
        Assert.Equal(20 << 20, (int)(s.Entries[0].FreedBytes >> 20));
        Assert.Equal(2, fired);
    }

    [Fact] public void 超100条截断最旧()
    {
        var s = new CleanHistoryService(_path);
        for (int i = 0; i < 110; i++) s.Record(E(i));
        Assert.Equal(100, s.Entries.Count);
        Assert.Equal(109 << 20, (int)(s.Entries[0].FreedBytes >> 20));
    }

    [Fact] public void 持久化跨实例()
    {
        var s = new CleanHistoryService(_path);
        s.Record(E(42, CleanTrigger.RuleThreshold));
        var s2 = new CleanHistoryService(_path);
        Assert.Single(s2.Entries);
        Assert.Equal(CleanTrigger.RuleThreshold, s2.Entries[0].Trigger);
    }

    [Fact] public void 损坏文件回退空列表()
    {
        File.WriteAllText(_path, "not json");
        var s = new CleanHistoryService(_path);
        Assert.Empty(s.Entries);
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `"/c/Program Files/dotnet/dotnet.exe" test tests/AiMemoryManager.Tests --filter "AtomicFileTests|CleanHistoryServiceTests"`
Expected: 编译失败

- [ ] **Step 3: 实现**

枚举扩展、AppSettings 5 字段按头部。INativeMemoryApi 加 2 方法,NativeMemoryApi 实现:

```csharp
// TryTerminateProcess: OpenProcess(0x0001 /*PROCESS_TERMINATE*/, false, pid)
//   句柄无效→error=GetLastWin32Error,返回false;TerminateProcess(h,1)失败→取错误;CloseHandle 必执行
// GetWindowTitles: EnumWindows 回调,GetWindowThreadProcessId 过滤 pid,IsWindowVisible,
//   GetWindowTextLength>0 时 GetWindowText 收标题;收集到 List<string>
```
(P/Invoke 声明照 M1 既有风格:EnumWindows 用委托回调 `[return: MarshalAs(UnmanagedType.Bool)]`,GetWindowText 用 StringBuilder。)

FakeNativeMemoryApi 加:`public Func<int, (bool, int)>? TerminateBehavior` 与 `public List<string> WindowTitles`/`Dictionary<int,List<string>>` 由测试控制(实现者按 M1 fake 风格最小实现)。

`Services/AtomicFile.cs`:
```csharp
namespace AiMemoryManager.Services;

public static class AtomicFile
{
    public static void WriteAllText(string path, string contents)
    {
        Directory.CreateDirectory(Path.GetDirectoryName(path)!);
        var tmp = path + ".tmp";
        File.WriteAllText(tmp, contents);
        File.Move(tmp, path, overwrite: true);
    }
}
```

`Services/CleanHistoryService.cs`:
```csharp
using System.Text.Json;
using AiMemoryManager.Models;

namespace AiMemoryManager.Services;

public class CleanHistoryService
{
    public const int Capacity = 100;

    private readonly string _path;
    private readonly LinkedList<CleanHistoryEntry> _entries = new();

    public IReadOnlyList<CleanHistoryEntry> Entries => _entries.ToList();
    public event EventHandler? Changed;

    public CleanHistoryService(string filePath)
    {
        _path = filePath;
        try
        {
            if (File.Exists(_path))
                foreach (var e in JsonSerializer.Deserialize<List<CleanHistoryEntry>>(File.ReadAllText(_path)) ?? new())
                    _entries.AddLast(e);
        }
        catch { /* 损坏 → 空 */ }
    }

    public static string DefaultPath() =>
        Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData),
                     "AiMemoryManager", "clean-history.json");

    public void Record(CleanHistoryEntry entry)
    {
        _entries.AddFirst(entry);
        while (_entries.Count > Capacity) _entries.RemoveLast();
        try { AtomicFile.WriteAllText(_path, JsonSerializer.Serialize(_entries.ToList())); }
        catch { /* 写失败不致命 */ }
        Changed?.Invoke(this, EventArgs.Empty);
    }
}
```

- [ ] **Step 4: 运行确认通过** → 7 passed;全量 105 passed(98+7)

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "M3基础:CleanLevel.L3/CleanTrigger.Analysis枚举、终止与窗口标题原生接口、清理历史服务、原子写助手(TDD)"
```

---

### Task 2: 防误杀名单 FR-7.2(TDD)

**Files:**
- Modify: `src/AiMemoryManager/Services/WhitelistService.cs`, `src/AiMemoryManager/Services/AnalysisService.cs`
- Test: `tests/AiMemoryManager.Tests/WhitelistNoKillTests.cs`, `tests/AiMemoryManager.Tests/AnalysisServiceTests.cs`(加 1 例)

**Interfaces:**
- Produces: 头部 WhitelistService 扩展 4 成员。AnalysisService 输出侧过滤:terminate 建议额外按 IsNoKill 过滤(compress 不受影响)。

- [ ] **Step 1: 写失败测试**

`WhitelistNoKillTests.cs`:
```csharp
using AiMemoryManager.Services;

namespace AiMemoryManager.Tests;

public class WhitelistNoKillTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "amm-test-" + Guid.NewGuid().ToString("N"));
    private readonly WhitelistService _wl;
    public WhitelistNoKillTests()
    {
        Directory.CreateDirectory(_dir);
        var settings = new SettingsService(Path.Combine(_dir, "s.json"));
        settings.Load();
        _wl = new WhitelistService(settings);
    }
    public void Dispose() => Directory.Delete(_dir, true);

    [Fact] public void 默认名单为空()
    {
        Assert.False(_wl.IsNoKill("chrome"));
        Assert.Empty(_wl.NoKill);
    }

    [Fact] public void 添加后命中且大小写不敏感()
    {
        _wl.AddNoKill("Game.exe");
        Assert.True(_wl.IsNoKill("game.exe"));
        Assert.True(_wl.IsNoKill("GAME.EXE"));
        Assert.Single(_wl.NoKill);
    }

    [Fact] public void 移除后不再命中()
    {
        _wl.AddNoKill("game");
        _wl.RemoveNoKill("GAME");
        Assert.False(_wl.IsNoKill("game"));
    }

    [Fact] public void 持久化到settings跨实例()
    {
        _wl.AddNoKill("game");
        var settings2 = new SettingsService(Path.Combine(_dir, "s.json"));
        settings2.Load();
        var wl2 = new WhitelistService(settings2);
        Assert.True(wl2.IsNoKill("game"));
    }

    [Fact] public void 防误杀与排除清理互不影响()
    {
        _wl.AddNoKill("game");
        Assert.False(_wl.IsExcluded("game"));   // 防误杀≠排除压缩
        _wl.Add("tool");
        Assert.False(_wl.IsNoKill("tool"));     // 排除清理≠防误杀
    }
}
```

`AnalysisServiceTests.cs` 追加(沿用该文件既有 fixture 模式,FakeLlmClient.Reply 改为含 terminate 建议):
```csharp
    [Fact] public async Task 防误杀名单过滤terminate建议()
    {
        _wl.AddNoKill("game");
        _client.Reply = """{"suggestions":[{"process":"game","action":"terminate","reason":"可关","risk":"low"},{"process":"chrome","action":"compress","reason":"高","risk":"low"}]}""";
        var r = await _svc.AnalyzeAsync(AnalysisTrigger.Manual);
        var s = Assert.Single(r.Suggestions);      // game 的 terminate 被滤掉
        Assert.Equal("chrome", s.ProcessName);
    }
```
注意:fixture 中 chrome 需不在白名单/game 需在快照中,实施者按既有 fixture 最小调整(加 game 进程快照)并注释。

- [ ] **Step 2: 运行确认失败** → 编译失败(IsNoKill 不存在)

- [ ] **Step 3: 实现**

WhitelistService 照 Excluded 的 volatile 快照模式复制一套 NoKill(读写 `_settings.Current.NoKillProcesses`,Save 触发重建,OrdinalIgnoreCase)。AnalysisService 输出侧过滤链加:
```csharp
.Where(s => s.Action != "terminate" || !_whitelist.IsNoKill(s.ProcessName))
```

- [ ] **Step 4: 运行确认通过** → 6 passed;全量 111 passed

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "防误杀名单(FR-7.2):NoKill存储与判定、大模型terminate建议过滤(TDD)"
```

---

### Task 3: UnsavedStateDetector FR-2.6(TDD)

**Files:**
- Create: `src/AiMemoryManager/Services/UnsavedStateDetector.cs`
- Test: `tests/AiMemoryManager.Tests/UnsavedStateDetectorTests.cs`

- [ ] **Step 1: 写失败测试**

```csharp
using AiMemoryManager.Services;
using AiMemoryManager.Tests.Fakes;

namespace AiMemoryManager.Tests;

public class UnsavedStateDetectorTests
{
    [Fact] public void 标题含星号判为未保存()
    {
        var fake = new FakeNativeMemoryApi();
        fake.SetWindowTitles(1, new List<string> { "无标题 - 记事本*", "其他窗口" });
        Assert.True(new UnsavedStateDetector(fake).HasUnsavedSigns(1));
    }

    [Fact] public void 标题含圆点判为未保存()
    {
        var fake = new FakeNativeMemoryApi();
        fake.SetWindowTitles(1, new List<string> { "文档• - Word" });
        Assert.True(new UnsavedStateDetector(fake).HasUnsavedSigns(1));
    }

    [Fact] public void 正常标题不误报()
    {
        var fake = new FakeNativeMemoryApi();
        fake.SetWindowTitles(1, new List<string> { "文档 - Word", "设置" });
        Assert.False(new UnsavedStateDetector(fake).HasUnsavedSigns(1));
    }

    [Fact] public void 无窗口进程不误报()
    {
        var fake = new FakeNativeMemoryApi();
        Assert.False(new UnsavedStateDetector(fake).HasUnsavedSigns(999));
    }
}
```
(FakeNativeMemoryApi 若 Task 1 用的是 Dictionary 而非 SetWindowTitles 方法,实施者按 Task 1 实际产物最小调整并注释。)

- [ ] **Step 2: 运行确认失败** → 编译失败

- [ ] **Step 3: 实现**

```csharp
using AiMemoryManager.Native;

namespace AiMemoryManager.Services;

public class UnsavedStateDetector
{
    private static readonly char[] Marks = { '*', '•' };
    private readonly INativeMemoryApi _native;

    public UnsavedStateDetector(INativeMemoryApi native) => _native = native;

    public bool HasUnsavedSigns(int pid) =>
        _native.GetWindowTitles(pid).Any(t => t.IndexOfAny(Marks) >= 0);
}
```

- [ ] **Step 4: 运行确认通过** → 4 passed;全量 115 passed

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "未保存检测(FR-2.6):窗口标题星号/圆点启发式(TDD)"
```

---

### Task 4: KillLogService 后悔药 + ProcessTerminateService L3 执行端(TDD)

**Files:**
- Create: `src/AiMemoryManager/Models/KillRecord.cs`, `src/AiMemoryManager/Models/TerminateResult.cs`, `src/AiMemoryManager/Services/KillLogService.cs`, `src/AiMemoryManager/Services/ProcessTerminateService.cs`
- Test: `tests/AiMemoryManager.Tests/KillLogServiceTests.cs`, `tests/AiMemoryManager.Tests/ProcessTerminateServiceTests.cs`

**Interfaces:**
- Produces: 头部 KillLogService/ProcessTerminateService 签名。命令行提供器默认实现(WMI)在 Task 4 Step 3 给出;测试注入假提供器。

- [ ] **Step 1: 写失败测试**

`KillLogServiceTests.cs`:
```csharp
using AiMemoryManager.Models;
using AiMemoryManager.Services;

namespace AiMemoryManager.Tests;

public class KillLogServiceTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "amm-test-" + Guid.NewGuid().ToString("N"));
    private readonly string _path;
    public KillLogServiceTests() { Directory.CreateDirectory(_dir); _path = Path.Combine(_dir, "k.json"); }
    public void Dispose() => Directory.Delete(_dir, true);

    private static KillRecord R(int pid, string? path = @"C:\apps\x.exe") =>
        new(DateTimeOffset.Now, pid, "x", path, null);

    [Fact] public void 记录新在前且超20截断()
    {
        var s = new KillLogService(_path);
        for (int i = 1; i <= 25; i++) s.Record(R(i));
        Assert.Equal(20, s.Records.Count);
        Assert.Equal(25, s.Records[0].Pid);
    }

    [Fact] public void 命令行由提供器补全()
    {
        var s = new KillLogService(_path, commandLineProvider: pid => pid == 7 ? "\"C:\\apps\\x.exe\" /fast" : null);
        s.Record(R(7));
        Assert.Equal("\"C:\\apps\\x.exe\" /fast", s.Records[0].Arguments);
        s.Record(R(8));
        Assert.Null(s.Records[1].Arguments);
    }

    [Fact] public void Restart调用starter并返回true()
    {
        string? gotPath = null, gotArgs = null;
        var s = new KillLogService(_path, starter: (p, a) => { gotPath = p; gotArgs = a; });
        var rec = R(1) with { Arguments = "/fast" };
        // Path 指向的文件须存在:
        var realExe = Path.Combine(_dir, "x.exe");
        File.WriteAllText(realExe, "dummy");
        rec = rec with { Path = realExe };
        Assert.True(s.Restart(rec));
        Assert.Equal(realExe, gotPath);
        Assert.Equal("/fast", gotArgs);
    }

    [Fact] public void Restart文件不存在返回false不调starter()
    {
        bool called = false;
        var s = new KillLogService(_path, starter: (_, _) => called = true);
        Assert.False(s.Restart(R(1, @"C:\不存在\x.exe")));
        Assert.False(called);
    }

    [Fact] public void 持久化跨实例()
    {
        var s = new KillLogService(_path);
        s.Record(R(3));
        Assert.Single(new KillLogService(_path).Records);
    }
}
```

`ProcessTerminateServiceTests.cs`:
```csharp
using AiMemoryManager.Models;
using AiMemoryManager.Services;
using AiMemoryManager.Tests.Fakes;

namespace AiMemoryManager.Tests;

public class ProcessTerminateServiceTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "amm-test-" + Guid.NewGuid().ToString("N"));
    private readonly FakeNativeMemoryApi _native;
    private readonly WhitelistService _wl;
    private readonly KillLogService _killLog;
    private readonly ProcessTerminateService _svc;
    private readonly List<int> _terminated = new();

    public ProcessTerminateServiceTests()
    {
        Directory.CreateDirectory(_dir);
        var settings = new SettingsService(Path.Combine(_dir, "s.json"));
        settings.Load();
        _wl = new WhitelistService(settings);
        _native = new FakeNativeMemoryApi
        {
            Processes =
            {
                new(1, "chrome", @"C:\chrome.exe", 900L << 20, true),
                new(2, "csrss", null, 200L << 20, false),
                new(3, "myapp", null, 300L << 20, true),
                new(4, "game", null, 500L << 20, true),
            },
            ForegroundPid = -1
        };
        _native.TerminateBehavior = pid => { _terminated.Add(pid); return (true, 0); };
        _killLog = new KillLogService(Path.Combine(_dir, "k.json"));
        _svc = new ProcessTerminateService(_native, _wl,
            new ForegroundGuard(_native, () => 999), new UnsavedStateDetector(_native), _killLog);
        _wl.Add("myapp");        // 排除清理白名单
        _wl.AddNoKill("game");   // 防误杀
    }
    public void Dispose() => Directory.Delete(_dir, true);

    [Fact] public async Task 四重防护_系统关键白名单防误杀前台均被拒()
    {
        _native.ForegroundPid = 1;   // chrome 变前台
        var r = await _svc.TerminateAsync(new[] { 1, 2, 3, 4 });
        Assert.Empty(_terminated);              // 全部被拒
        Assert.Empty(r.Items);
    }

    [Fact] public async Task 正常进程被终止且记后悔药()
    {
        var r = await _svc.TerminateAsync(new[] { 1 });
        Assert.Equal(new[] { 1 }, _terminated);
        Assert.Single(r.Items);
        Assert.True(r.Items[0].Success);
        Assert.Single(_killLog.Records);
        Assert.Equal("chrome", _killLog.Records[0].Name);
        Assert.Equal(900L << 20, r.FreedBytes);  // 按快照工作集估算
    }

    [Fact] public async Task 终止失败带错误码且不记后悔药()
    {
        _native.TerminateBehavior = _ => (false, 5);
        var r = await _svc.TerminateAsync(new[] { 1 });
        Assert.False(r.Items[0].Success);
        Assert.Equal(5, r.Items[0].Win32Error);
        Assert.Empty(_killLog.Records);
    }

    [Fact] public void FilterCandidates排除四类但保留正常()
    {
        _native.ForegroundPid = 1;
        var kept = _svc.FilterCandidates(new[] { 1, 2, 3, 4 });
        Assert.Empty(kept);
        _native.ForegroundPid = -1;
        kept = _svc.FilterCandidates(new[] { 1 });
        Assert.Equal(new[] { 1 }, kept);
    }

    [Fact] public async Task TerminateCompleted事件触发()
    {
        TerminateResult? got = null;
        _svc.TerminateCompleted += (_, r) => got = r;
        await _svc.TerminateAsync(new[] { 1 });
        Assert.NotNull(got);
    }
}
```

- [ ] **Step 2: 运行确认失败** → 编译失败

- [ ] **Step 3: 实现**

`Models/KillRecord.cs`、`Models/TerminateResult.cs` 按头部。

`Services/KillLogService.cs`:
```csharp
using System.Diagnostics;
using System.Text.Json;
using AiMemoryManager.Models;

namespace AiMemoryManager.Services;

public class KillLogService
{
    public const int Capacity = 20;

    private readonly string _path;
    private readonly Func<int, string?> _cmdline;
    private readonly Action<string, string?> _starter;
    private readonly LinkedList<KillRecord> _records = new();

    public IReadOnlyList<KillRecord> Records => _records.ToList();

    public KillLogService(string filePath, Func<int, string?>? commandLineProvider = null, Action<string, string?>? starter = null)
    {
        _path = filePath;
        _cmdline = commandLineProvider ?? WmiCommandLine;
        _starter = starter ?? DefaultStart;
        try
        {
            if (File.Exists(_path))
                foreach (var r in JsonSerializer.Deserialize<List<KillRecord>>(File.ReadAllText(_path)) ?? new())
                    _records.AddLast(r);
        }
        catch { }
    }

    public static string DefaultPath() =>
        Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData),
                     "AiMemoryManager", "kill-log.json");

    public void Record(KillRecord record)
    {
        string? args = record.Arguments;
        if (args == null)
        {
            try { args = _cmdline(record.Pid); } catch { args = null; }
        }
        _records.AddFirst(record with { Arguments = args });
        while (_records.Count > Capacity) _records.RemoveLast();
        try { AtomicFile.WriteAllText(_path, JsonSerializer.Serialize(_records.ToList())); } catch { }
    }

    public bool Restart(KillRecord record)
    {
        if (string.IsNullOrEmpty(record.Path) || !File.Exists(record.Path)) return false;
        try { _starter(record.Path, record.Arguments); return true; }
        catch { return false; }
    }

    private static void DefaultStart(string path, string? args) =>
        Process.Start(new ProcessStartInfo(path) { Arguments = args ?? "", UseShellExecute = true });

    private static string? WmiCommandLine(int pid)
    {
        // System.Management 包(Task 4 Step 3 先 dotnet add):select CommandLine from Win32_Process
        using var searcher = new System.Management.ManagementObjectSearcher(
            $"SELECT CommandLine FROM Win32_Process WHERE ProcessId = {pid}");
        foreach (var mo in searcher.Get())
            return mo["CommandLine"]?.ToString();
        return null;
    }
}
```
先加包:`"/c/Program Files/dotnet/dotnet.exe" add src/AiMemoryManager package System.Management`

`Services/ProcessTerminateService.cs`:
```csharp
using AiMemoryManager.Models;
using AiMemoryManager.Native;

namespace AiMemoryManager.Services;

public class ProcessTerminateService
{
    private readonly INativeMemoryApi _native;
    private readonly WhitelistService _whitelist;
    private readonly ForegroundGuard _guard;
    private readonly UnsavedStateDetector _detector;   // 预留:确认对话框在 UI 层用,执行端不重复
    private readonly KillLogService _killLog;

    public event EventHandler<TerminateResult>? TerminateCompleted;

    public ProcessTerminateService(INativeMemoryApi native, WhitelistService whitelist,
        ForegroundGuard guard, UnsavedStateDetector detector, KillLogService killLog)
        => (_native, _whitelist, _guard, _detector, _killLog) = (native, whitelist, guard, detector, killLog);

    public IReadOnlyList<int> FilterCandidates(IReadOnlyCollection<int> pids)
    {
        var names = _native.GetProcessSnapshots().ToDictionary(p => p.Pid, p => p.Name);
        return pids.Where(pid =>
        {
            if (!names.TryGetValue(pid, out var name)) return false;
            if (_whitelist.IsSystemCritical(name)) return false;
            if (_whitelist.IsExcluded(name)) return false;
            if (_whitelist.IsNoKill(name)) return false;
            if (_guard.IsProtected(pid)) return false;
            return true;
        }).ToList();
    }

    public Task<TerminateResult> TerminateAsync(IReadOnlyCollection<int> pids)
        => Task.Run(() =>
        {
            var snaps = _native.GetProcessSnapshots().ToDictionary(p => p.Pid);
            var items = new List<TerminateItemResult>();
            long freed = 0;
            foreach (var pid in FilterCandidates(pids))
            {
                var snap = snaps[pid];
                bool ok = _native.TryTerminateProcess(pid, out int err);
                items.Add(new TerminateItemResult(pid, snap.Name, ok, ok ? 0 : err));
                if (ok)
                {
                    freed += snap.WorkingSetBytes;
                    _killLog.Record(new KillRecord(DateTimeOffset.Now, pid, snap.Name, snap.Path, null));
                }
            }
            var result = new TerminateResult(freed, items);
            TerminateCompleted?.Invoke(this, result);
            return result;
        });
}
```

- [ ] **Step 4: 运行确认通过** → 10 passed;全量 125 passed

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "L3执行端与后悔药(FR-2.3/2.7):四重防护强制重查、终止结果、20条后悔药日志、WMI命令行(TDD)"
```

---

### Task 5: StartupService FR-8.3 + 通知开关 FR-8.4

**Files:**
- Create: `src/AiMemoryManager/Services/StartupService.cs`
- Modify: `src/AiMemoryManager/MainWindow.xaml.cs`(3 处 ShowNotification 加开关), `src/AiMemoryManager/Services/Locator.cs`, `src/AiMemoryManager/ViewModels/SettingsViewModel.cs`, `src/AiMemoryManager/Views/SettingsPage.xaml`, i18n 双文件
- Test: `tests/AiMemoryManager.Tests/StartupServiceTests.cs`(仅测试可注入的注册表抽象)

**Interfaces:**
- Produces: `Locator.Startup`、`Locator.History` 之外的 Startup 服务;设置页 AutoStart/Notifications 开关。

设计决策(以此为准):StartupService 注入 `Action<string, string?> setRunKey` / `Func<string, string?> getRunKey` 两个委托抽象注册表,默认实现用 Microsoft.Win32.Registry HKCU Run 键(值名 "AiMemoryManager",值为带引号的 exe 路径);打包态(MSIX)后续 M4 加 StartupTask 声明,本任务 `IsPackaged` 检测走 try 读 `Windows.ApplicationModel.Package.Current` 异常即未打包,未打包用注册表。

- [ ] **Step 1: 写失败测试**

```csharp
using AiMemoryManager.Services;

namespace AiMemoryManager.Tests;

public class StartupServiceTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "amm-test-" + Guid.NewGuid().ToString("N"));
    private readonly Dictionary<string, string?> _runKey = new();
    private readonly SettingsService _settings;
    private readonly StartupService _svc;

    public StartupServiceTests()
    {
        Directory.CreateDirectory(_dir);
        _settings = new SettingsService(Path.Combine(_dir, "s.json"));
        _settings.Load();
        _svc = new StartupService(_settings,
            setRunKey: (name, value) => _runKey[name] = value,
            getRunKey: name => _runKey.GetValueOrDefault(name));
    }
    public void Dispose() => Directory.Delete(_dir, true);

    [Fact] public void 默认未启用()
    {
        Assert.False(_svc.IsEnabled);
    }

    [Fact] public void 启用写注册表与设置()
    {
        _svc.SetEnabled(true);
        Assert.True(_svc.IsEnabled);
        Assert.NotNull(_runKey["AiMemoryManager"]);
        Assert.True(_settings.Current.AutoStartEnabled);
    }

    [Fact] public void 禁用清注册表与设置()
    {
        _svc.SetEnabled(true);
        _svc.SetEnabled(false);
        Assert.False(_svc.IsEnabled);
        Assert.Null(_runKey["AiMemoryManager"]);
        Assert.False(_settings.Current.AutoStartEnabled);
    }
}
```

- [ ] **Step 2: 运行确认失败** → 编译失败

- [ ] **Step 3: 实现**

```csharp
using System.Diagnostics;
using Microsoft.Win32;

namespace AiMemoryManager.Services;

public class StartupService
{
    private const string RunKeyName = "AiMemoryManager";
    private const string RunKeyPath = @"Software\Microsoft\Windows\CurrentVersion\Run";

    private readonly SettingsService _settings;
    private readonly Action<string, string?> _setRunKey;
    private readonly Func<string, string?> _getRunKey;

    public StartupService(SettingsService settings,
        Action<string, string?>? setRunKey = null, Func<string, string?>? getRunKey = null)
    {
        _settings = settings;
        _setRunKey = setRunKey ?? DefaultSet;
        _getRunKey = getRunKey ?? DefaultGet;
    }

    public bool IsEnabled => _getRunKey(RunKeyName) != null;

    public void SetEnabled(bool enabled)
    {
        try
        {
            _setRunKey(RunKeyName, enabled ? $"\"{Environment.ProcessPath}\"" : null);
            _settings.Current.AutoStartEnabled = enabled;
            _settings.Save();
        }
        catch (Exception ex) { Debug.WriteLine("StartupService: " + ex); }
    }

    private static void DefaultSet(string name, string? value)
    {
        using var key = Registry.CurrentUser.OpenSubKey(RunKeyPath, writable: true);
        if (value == null) key?.DeleteValue(name, throwOnMissingValue: false);
        else key?.SetValue(name, value);
    }

    private static string? DefaultGet(string name)
    {
        using var key = Registry.CurrentUser.OpenSubKey(RunKeyPath, writable: false);
        return key?.GetValue(name)?.ToString();
    }
}
```

MainWindow 3 处 ShowNotification 调用点统一前置:
```csharp
if (!Locator.Settings.Current.NotificationsEnabled) return; // 各处理器入口
```
(托盘气泡由系统通知设置控制是另一层;本开关是应用内总闸,FR-8.4"可单独开关"。)

Locator 加 `public static StartupService Startup`、`public static CleanHistoryService History`、`public static KillLogService KillLog`、`public static UnsavedStateDetector Unsaved`、`public static ProcessTerminateService Terminator`,Init 末尾接线(顺序:History/KillLog/Unsaved → Terminator → Startup)。**只有 CleanService 在 Locator 接线写历史**:
```csharp
Clean.CleanCompleted += (_, r) => History.Record(new CleanHistoryEntry(r.Time, r.Level, r.FreedBytes, r.ProcessCount, r.Trigger));
```
Terminator 的历史**不由 Locator 记**,由调用方 VM 在 TerminateAsync 成功后记(进程页记 `CleanTrigger.Manual`、分析页记 `CleanTrigger.Analysis`,Level=L3),避免双记且触发方式准确。

SettingsViewModel 加 AutoStartEnabled(setter 调 `Locator.Startup.SetEnabled(value)` 再 OnPropertyChanged 回读 IsEnabled)与 NotificationsEnabled(即时存储);SettingsPage 加两个 ToggleSwitch,i18n key:`Settings.AutoStart`/`Settings.AutoStartDesc`/`Settings.Notifications`/`Settings.NotificationsDesc` 双语。

- [ ] **Step 4: 构建+测试+冒烟** → 3 passed;全量 128 passed;build 0 error;10s 冒烟

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "开机自启与通知开关(FR-8.3/8.4):注册表Run键、设置页开关、清理历史接线"
```

---

### Task 6: HotkeyService FR-8.5 全局热键

**Files:**
- Create: `src/AiMemoryManager/Services/HotkeyService.cs`
- Modify: `src/AiMemoryManager/MainWindow.xaml.cs`(HwndSource hook + Pressed→L1), `src/AiMemoryManager/App.xaml.cs`(注册/注销), `src/AiMemoryManager/ViewModels/SettingsViewModel.cs`, `src/AiMemoryManager/Views/SettingsPage.xaml`(热键捕获框), i18n 双文件
- Test: `tests/AiMemoryManager.Tests/HotkeyServiceTests.cs`(WndProc 消息分发逻辑,不碰真注册)

- [ ] **Step 1: 写失败测试**

```csharp
using System.Windows.Interop;
using AiMemoryManager.Services;

namespace AiMemoryManager.Tests;

public class HotkeyServiceTests
{
    private const int WM_HOTKEY = 0x0312;

    [Fact] public void WM_HOTKEY消息触发Pressed事件()
    {
        var s = new HotkeyService();
        int fired = 0;
        s.Pressed += (_, _) => fired++;
        bool handled = false;
        s.WndProc(IntPtr.Zero, WM_HOTKEY, new IntPtr(HotkeyService.HotkeyId), IntPtr.Zero, ref handled);
        Assert.Equal(1, fired);
        Assert.True(handled);
    }

    [Fact] public void 其他消息不触发()
    {
        var s = new HotkeyService();
        int fired = 0;
        s.Pressed += (_, _) => fired++;
        bool handled = false;
        s.WndProc(IntPtr.Zero, 0x0200 /*WM_MOUSEMOVE*/, IntPtr.Zero, IntPtr.Zero, ref handled);
        Assert.Equal(0, fired);
        Assert.False(handled);
    }

    [Fact] public void 未注册时Register返回依赖系统_测试中跳过真实注册()
    {
        // 仅验证 Dispose/Unregister 幂等不抛
        var s = new HotkeyService();
        s.Unregister();
        s.Dispose();
        s.Dispose();
    }
}
```

- [ ] **Step 2: 运行确认失败** → 编译失败

- [ ] **Step 3: 实现**

```csharp
using System.Runtime.InteropServices;

namespace AiMemoryManager.Services;

public class HotkeyService : IDisposable
{
    public const int HotkeyId = 0xA4M1 & 0xFFFF;   // 见下,实施时用 0xA401 常量
    private const int WM_HOTKEY = 0x0312;

    [DllImport("user32.dll", SetLastError = true)][return: MarshalAs(UnmanagedType.Bool)]
    private static extern bool RegisterHotKey(IntPtr hWnd, int id, uint fsModifiers, uint vk);
    [DllImport("user32.dll", SetLastError = true)][return: MarshalAs(UnmanagedType.Bool)]
    private static extern bool UnregisterHotKey(IntPtr hWnd, int id);

    private IntPtr _hwnd;
    private bool _registered;

    public event EventHandler? Pressed;

    public bool Register(IntPtr hwnd, int modifiers, int key)
    {
        Unregister();
        _registered = RegisterHotKey(hwnd, HotkeyId, (uint)modifiers, (uint)key);
        _hwnd = _registered ? hwnd : IntPtr.Zero;
        return _registered;
    }

    public void Unregister()
    {
        if (_registered) { UnregisterHotKey(_hwnd, HotkeyId); _registered = false; _hwnd = IntPtr.Zero; }
    }

    public IntPtr WndProc(IntPtr hwnd, int msg, IntPtr wParam, IntPtr lParam, ref bool handled)
    {
        if (msg == WM_HOTKEY && wParam.ToInt32() == HotkeyId)
        {
            handled = true;
            Pressed?.Invoke(this, EventArgs.Empty);
        }
        return IntPtr.Zero;
    }

    public void Dispose() => Unregister();
}
```
(HotkeyId 用 `public const int HotkeyId = 0xA401;`,上面注释行删掉。)

接线:MainWindow OnSourceInitialized(若无则加)里 `HwndSource.FromHwnd(...).AddHook(Locator.Hotkey.WndProc)`;App OnStartup 主窗口创建后 `Locator.Hotkey.Register(helper.Handle, Settings.Current.HotkeyModifiers, Settings.Current.HotkeyKey)`(WindowInteropHelper);Pressed 订阅→Dispatcher 封送→`await Locator.Clean.RunL1Async(CleanTrigger.Manual)`(try/catch);OnExit Unregister/Dispose。

设置页热键捕获:只读 TextBox,PreviewKeyDown 里取 `Keyboard.Modifiers`+`e.Key` 转 Win32 修饰符/VK,写 Settings.HotkeyModifiers/HotkeyKey 并重新 Register;显示文本如 "Ctrl+Shift+M"。i18n key:`Settings.Hotkey`/`Settings.HotkeyHint`/`Settings.HotkeyFail` 双语。MOD 常量:MOD_ALT=0x1, MOD_CONTROL=0x2, MOD_SHIFT=0x4, MOD_WIN=0x8;Key→VK 用 `KeyInterop.VirtualKeyFromKey`。

- [ ] **Step 4: 构建+测试+冒烟** → 3 passed;全量 131 passed;冒烟时按 Ctrl+Shift+M 人工项入清单

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "全局热键(FR-8.5):注册/WndProc分发/设置页捕获改键,触发一键L1"
```

---

### Task 7: 进程页 L3 UI + 确认对话框 + 后悔药 UI

**Files:**
- Create: `src/AiMemoryManager/Views/TerminateConfirmDialog.xaml(.cs)`
- Modify: `src/AiMemoryManager/ViewModels/ProcessesViewModel.cs`, `src/AiMemoryManager/Views/ProcessesPage.xaml(.cs)`, `src/AiMemoryManager/ViewModels/WhitelistViewModel.cs`, `src/AiMemoryManager/Views/WhitelistPage.xaml`(防误杀名单第二列表), `src/AiMemoryManager/ViewModels/DashboardViewModel.cs`, `src/AiMemoryManager/Views/DashboardPage.xaml`(清理历史卡), `src/AiMemoryManager/Services/Locator.cs`(若 Task 5 未挂 Terminator/KillLog/Unsaved), i18n 双文件

**Interfaces:**
- Consumes: `Locator.Terminator`(FilterCandidates/TerminateAsync)、`Locator.Unsaved.HasUnsavedSigns`、`Locator.KillLog`(Records/Restart)、`Locator.Native.GetProcessSnapshots`

- [ ] **Step 1: TerminateConfirmDialog(WPF-UI ContentDialog 或 FluentWindow,照 M1 对话框先例)**

内容:
- 标题 `L3.ConfirmTitle`
- 进程清单列表:名称、内存 MB、路径;**命中未保存检测的行加 `L3.HighRisk` 红色标记**(`Locator.Unsaved.HasUnsavedSigns(pid)` 在打开对话框时逐个查,结果传入)
- 汇总行:`L3.ConfirmSummary`(将结束 {0} 个进程,预计释放 {1} MB)
- 含高风险进程时追加警告 `L3.HighRiskWarning`(检测到未保存迹象,结束可能丢失数据)
- 按钮:确认(主,`L3.Confirm`)/ 取消
- 暴露 `IReadOnlyList<int> SelectedPids` 与确认结果

- [ ] **Step 2: ProcessesViewModel 扩展**

- 每行进程加 `bool IsSelected`(勾选)、`bool CanKill`(非系统关键/非白名单/非防误杀/非前台 — 用 `Locator.Terminator.FilterCandidates` 批量算一次再映射回行,避免逐行快照)
- `[RelayCommand] TerminateSelectedAsync`:勾选的 CanKill 行 → 开 TerminateConfirmDialog → 确认后 `Locator.Terminator.TerminateAsync` → **成功后记历史**:`Locator.History.Record(new CleanHistoryEntry(DateTimeOffset.Now, CleanLevel.L3, r.FreedBytes, 成功数, CleanTrigger.Manual))` → 结果文本 `L3.Result`(成功 {0} 个,释放 {1} MB;失败 {2} 个)→ 刷新列表
- 后悔药区:`ObservableCollection<KillRecord> KillLogRecords`(订阅或手动刷新),`[RelayCommand] RestoreAsync(KillRecord)` → `Locator.KillLog.Restart` → 成功 `L3.Restored`/失败 `L3.RestoreFailed` 状态文本
- 系统关键/受保护行勾选框禁用(灰显)

- [ ] **Step 3: ProcessesPage XAML**

- 列表每行头部加 CheckBox(绑 IsSelected,IsEnabled 绑 CanKill)
- 顶部/底部操作条:"结束选中进程"按钮(`L3.Terminate`,危险色 Accent),勾选的 CanKill 数为 0 时禁用
- 后悔药卡:标题 `L3.UndoTitle`,KillRecord 列表(时间/名称/路径),每行"恢复"按钮 `L3.Restore`;空态 `L3.UndoEmpty`
- **白名单页防误杀列表**:WhitelistPage 现有"排除清理"列表下方加第二卡 `WL.NoKillTitle`,列表+添加/删除(照现有列表模式,调 `Locator.Whitelist.AddNoKill/RemoveNoKill`),说明文案 `WL.NoKillDesc`
- **仪表盘清理历史卡**:DashboardViewModel 加 `IEnumerable<CleanHistoryEntry> HistoryEntries`(绑 `Locator.History.Entries`,订阅 `Changed` 事件 Dispatcher 封送刷新,页面 Unloaded 退订);DashboardPage 加卡片 `Dash.History`,行列:时间/级别/释放 MB/触发方式(级别与触发经 i18n:`Dash.Level.L1/L2/L3`、`Tokens.Trigger.*` 复用或新 key `Dash.Trigger.*`)
- i18n key 批量(双语):`L3.Terminate/L3.ConfirmTitle/L3.ConfirmSummary/L3.HighRisk/L3.HighRiskWarning/L3.Confirm/L3.Cancel/L3.Result/L3.UndoTitle/L3.Restore/L3.Restored/L3.RestoreFailed/L3.UndoEmpty/WL.NoKillTitle/WL.NoKillDesc/Dash.History/Dash.Level.L1/Dash.Level.L2/Dash.Level.L3`

- [ ] **Step 4: 构建+测试+冒烟** → 全量 131 passed 不变;build 0 error;10s 冒烟

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "进程页L3(FR-2.3/2.6/2.7):勾选+确认对话框(未保存高风险标记)+后悔药恢复"
```

---

### Task 8: 智能分析页 terminate 建议接通

**Files:**
- Modify: `src/AiMemoryManager/ViewModels/SmartAnalysisViewModel.cs`, `src/AiMemoryManager/Views/SmartAnalysisPage.xaml`, i18n 双文件(删 `Analysis.TerminateM3`?不删,保留无妨 — 改为不再使用即可;新增 `Analysis.TerminateNow`)

- [ ] **Step 1: 建议卡 terminate 行改造**

SuggestionItemViewModel:`CanExecute` 扩展为 compress 或 terminate 均可执行;terminate 行按钮文本 `Analysis.TerminateNow`(替换灰显 M3 文案)。

- [ ] **Step 2: 执行流**

`[RelayCommand] TerminateAsync(SuggestionItemViewModel? item)`:按进程名从快照取 pid 集(同名多实例全取)→ `Locator.Terminator.FilterCandidates` → 空则状态文本 `Analysis.NoneKillable` → 非空开 TerminateConfirmDialog(Task 7 产物,复用)→ 确认后 TerminateAsync → **成功后记历史**:`Locator.History.Record(new CleanHistoryEntry(DateTimeOffset.Now, CleanLevel.L3, r.FreedBytes, 成功数, CleanTrigger.Analysis))` → 结果文本(复用 `L3.Result`)→ 该卡标记已处理(从 Suggestions 移除或置灰)。

- [ ] **Step 3: i18n + 构建+测试+冒烟**

新 key:`Analysis.TerminateNow`(结束进程)/`Analysis.NoneKillable`(该进程受保护,不可结束)双语。全量 131 passed;10s 冒烟。

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "分析建议terminate接通L3:确认对话框复用、防误杀已过滤建议可一键执行"
```

---

### Task 9: SystemPathGuard + DiskScanService + DiskCandidateProvider(TDD)

**Files:**
- Create: `src/AiMemoryManager/Services/SystemPathGuard.cs`, `src/AiMemoryManager/Models/DiskScan.cs`, `src/AiMemoryManager/Services/DiskScanService.cs`, `src/AiMemoryManager/Services/DiskCandidateProvider.cs`
- Test: `tests/AiMemoryManager.Tests/SystemPathGuardTests.cs`, `tests/AiMemoryManager.Tests/DiskScanServiceTests.cs`

- [ ] **Step 1: 写失败测试**

`SystemPathGuardTests.cs`:
```csharp
using AiMemoryManager.Services;

namespace AiMemoryManager.Tests;

public class SystemPathGuardTests
{
    private static string Sys => Environment.GetFolderPath(Environment.SpecialFolder.System)
        is var s ? Path.GetPathRoot(s)! : @"C:\";   // 系统盘根,如 "C:\"

    [Fact] public void Windows目录及子路径受保护()
    {
        Assert.True(SystemPathGuard.IsProtected(Sys + @"Windows"));
        Assert.True(SystemPathGuard.IsProtected(Sys + @"Windows\System32"));
        Assert.True(SystemPathGuard.IsProtected(Sys + @"windows\temp"));   // 大小写不敏感
    }

    [Fact] public void ProgramFiles与ProgramData受保护()
    {
        Assert.True(SystemPathGuard.IsProtected(Sys + "Program Files"));
        Assert.True(SystemPathGuard.IsProtected(Sys + "Program Files (x86)"));
        Assert.True(SystemPathGuard.IsProtected(Sys + @"ProgramData\Microsoft"));
    }

    [Fact] public void 用户目录不受保护()
    {
        Assert.False(SystemPathGuard.IsProtected(Sys + @"Users\jerry\Downloads"));
        Assert.False(SystemPathGuard.IsProtected(@"D:\Games"));
    }

    [Fact] public void 前缀撞名不误判()
    {
        Assert.False(SystemPathGuard.IsProtected(Sys + @"WindowsOld"));       // 不是 Windows\
        Assert.False(SystemPathGuard.IsProtected(Sys + @"Program Files2"));   // 不是 Program Files\
    }
}
```

`DiskScanServiceTests.cs`:
```csharp
using AiMemoryManager.Models;
using AiMemoryManager.Services;

namespace AiMemoryManager.Tests;

public class DiskScanServiceTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "amm-test-" + Guid.NewGuid().ToString("N"));
    public DiskScanServiceTests()
    {
        Directory.CreateDirectory(_dir);
        File.WriteAllText(Path.Combine(_dir, "a.bin"), new string('x', 100));
        Directory.CreateDirectory(Path.Combine(_dir, "sub"));
        File.WriteAllText(Path.Combine(_dir, "sub", "b.bin"), new string('x', 200));
    }
    public void Dispose() => Directory.Delete(_dir, true);

    [Fact] public async Task 测量含子目录的文件总大小与计数()
    {
        var r = await new DiskScanService().MeasureAsync(_dir, DiskCategory.Other);
        Assert.Equal(300, r.SizeBytes);
        Assert.Equal(2, r.FileCount);
        Assert.Equal(DiskCategory.Other, r.Category);
    }

    [Fact] public async Task 不存在路径返回零()
    {
        var r = await new DiskScanService().MeasureAsync(Path.Combine(_dir, "nope"), DiskCategory.Temp);
        Assert.Equal(0, r.SizeBytes);
    }

    [Fact] public async Task ScanAsync批量且跳过失败项()
    {
        var list = await new DiskScanService().ScanAsync(new[]
        {
            new DiskCandidate(_dir, DiskCategory.UserFolder),
            new DiskCandidate(Path.Combine(_dir, "nope"), DiskCategory.Temp),
        });
        Assert.Equal(2, list.Count);
        Assert.Equal(300, list[0].SizeBytes);
    }
}
```

- [ ] **Step 2: 运行确认失败** → 编译失败

- [ ] **Step 3: 实现**

`SystemPathGuard`:
```csharp
namespace AiMemoryManager.Services;

public static class SystemPathGuard
{
    private static readonly string[] Protected =
    {
        @"Windows", @"Program Files", @"Program Files (x86)", @"ProgramData",
    };

    public static bool IsProtected(string path)
    {
        string root = Path.GetPathRoot(Environment.GetFolderPath(Environment.SpecialFolder.System))!;
        string full;
        try { full = Path.GetFullPath(path).TrimEnd(Path.DirectorySeparatorChar); }
        catch { return true; }   // 解析不了的路径按受保护处理(宁可不碰)
        foreach (var p in Protected)
        {
            string guard = Path.Combine(root, p);
            if (full.Equals(guard, StringComparison.OrdinalIgnoreCase)) return true;
            if (full.StartsWith(guard + Path.DirectorySeparatorChar, StringComparison.OrdinalIgnoreCase)) return true;
        }
        return false;
    }
}
```

`DiskScanService`:迭代栈遍历(不用递归防栈溢出),`new FileInfo(f).Length` 逐个 catch(UnauthorizedAccessException/IOException/PathTooLongException 跳过),返回 `FolderSizeInfo`。`ScanAsync` 顺序 MeasureAsync(磁盘 IO 串行避免抖动),ct 支持取消。

`DiskCandidateProvider.GetCandidates()`:
```csharp
// %TEMP%(Temp)
// Chrome: %LOCALAPPDATA%\Google\Chrome\User Data\Default\Cache(BrowserCache)
// Edge:   %LOCALAPPDATA%\Microsoft\Edge\User Data\Default\Cache(BrowserCache)
// 回收站: <系统盘>$Recycle.Bin(RecycleBin,MeasureAsync 访问失败自然得 0,UI 可忽略 0 项)
// %USERPROFILE% 下一级目录(Downloads/Documents/Desktop/Pictures/Videos/Music/AppData 等,
//   逐个 MeasureAsync 太慢 — 只列存在的一级目录为 UserFolder 候选,大小由 DiskScanService 统一量;
//   数量取全部存在者,UI 层量完按大小排序展示 Top15)
// 每个候选先排除 SystemPathGuard.IsProtected
```

- [ ] **Step 4: 运行确认通过** → 7 passed;全量 138 passed

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "C盘瘦身基础(FR-12.1/12.5):系统路径硬白名单、磁盘扫描、候选清单(TDD)"
```

---

### Task 10: DiskAdviceService FR-12.2(TDD)

**Files:**
- Create: `src/AiMemoryManager/Models/DiskAdvice.cs`, `src/AiMemoryManager/Services/DiskAdviceService.cs`
- Test: `tests/AiMemoryManager.Tests/DiskAdviceServiceTests.cs`

**Interfaces:**
- Consumes: M2 `LlmProfileService.GetActive` / `ILlmClient` / `TokenStatsService.Record` / `LocalizationService.CurrentLanguage`
- Produces: 头部 DiskAdviceService。提示词常量 `DiskAdviceService.PromptTemplate`(内嵌,不进 PromptTemplateService — YAGNI,磁盘分析单一用途)。

- [ ] **Step 1: 写失败测试**

```csharp
using AiMemoryManager.Models;
using AiMemoryManager.Services;

namespace AiMemoryManager.Tests;

public class DiskAdviceServiceTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "amm-test-" + Guid.NewGuid().ToString("N"));
    private readonly FakeClient _client = new();
    private readonly DiskAdviceService _svc;

    private sealed class FakeClient : ILlmClient
    {
        public string Reply = """
            {"cleanable":[{"path":"C:\\Users\\j\\AppData\\Local\\Temp","reason":"临时文件","estMB":1200},
                          {"path":"C:\\Windows\\System32","reason":"模型瞎说","estMB":1}],
             "migratable":[{"path":"C:\\Users\\j\\Videos","reason":"视频库大","target":"D:"},
                          {"path":"C:\\Users\\j\\NotInScan","reason":"不在扫描里","target":"D:"},
                          {"path":"C:\\Users\\j\\Games","reason":"游戏库","target":"Z:"}]}
            """;
        public string LastPrompt = "";
        public Task<LlmResponse> ChatAsync(LlmProfile profile, string system, string user, CancellationToken ct = default)
        { LastPrompt = user; return Task.FromResult(new LlmResponse(Reply, new LlmUsage(500, 200))); }
        public Task<IReadOnlyList<string>> ListModelsAsync(LlmProfile profile, CancellationToken ct = default)
            => Task.FromResult<IReadOnlyList<string>>(new[] { "m" });
    }

    public DiskAdviceServiceTests()
    {
        Directory.CreateDirectory(_dir);
        var settings = new SettingsService(Path.Combine(_dir, "s.json"));
        settings.Load();
        var profiles = new LlmProfileService(Path.Combine(_dir, "p.json"), settings);
        profiles.Load();
        profiles.Save(new LlmProfile { Id = "p1", Name = "ds", BaseUrl = "https://x/v1", Model = "m" });
        profiles.SetActive("p1");
        var l10n = new LocalizationService(Path.Combine(AppContext.BaseDirectory, "i18n"));
        _svc = new DiskAdviceService(profiles, _client, l10n,
            new TokenStatsService(Path.Combine(_dir, "u.jsonl"), () => DateTimeOffset.Now),
            new[] { "D:\\", "E:\\" });
    }
    public void Dispose() => Directory.Delete(_dir, true);

    private static IReadOnlyList<FolderSizeInfo> Scan() => new[]
    {
        new FolderSizeInfo(@"C:\Users\j\AppData\Local\Temp", 1200L << 20, 500, DiskCategory.Temp),
        new FolderSizeInfo(@"C:\Users\j\Videos", 50L << 30, 100, DiskCategory.UserFolder),
        new FolderSizeInfo(@"C:\Users\j\Games", 80L << 30, 200, DiskCategory.UserFolder),
        new FolderSizeInfo(@"C:\Windows\System32", 1L << 30, 999, DiskCategory.Other),
    };

    [Fact] public async Task 建议经系统路径与候选集双重过滤()
    {
        var a = await _svc.AnalyzeAsync(Scan());
        var c = Assert.Single(a.Cleanable);          // Windows\System32 被滤
        Assert.Contains("Temp", c.Path);
        Assert.Equal(1200L << 20, c.EstBytes);
        var m = Assert.Single(a.Migratable);         // NotInScan(不在候选)与 Z:(盘不存在)被滤
        Assert.Equal(@"C:\Users\j\Videos", m.Path);
        Assert.Equal("D:", m.TargetDrive);
    }

    [Fact] public async Task Token被记录()
    {
        await _svc.AnalyzeAsync(Scan());
        var stats = new TokenStatsService(Path.Combine(_dir, "u.jsonl"), () => DateTimeOffset.Now);
        Assert.Equal(500, stats.LoadAll().Sum(r => r.InputTokens));
    }

    [Fact] public async Task 无激活档案抛明确异常()
    {
        var settings = new SettingsService(Path.Combine(_dir, "s2.json"));
        settings.Load();
        var empty = new LlmProfileService(Path.Combine(_dir, "p2.json"), settings);
        empty.Load();
        var svc = new DiskAdviceService(empty, _client,
            new LocalizationService(Path.Combine(AppContext.BaseDirectory, "i18n")),
            new TokenStatsService(Path.Combine(_dir, "u2.jsonl"), () => DateTimeOffset.Now),
            new[] { "D:\\" });
        await Assert.ThrowsAsync<InvalidOperationException>(() => svc.AnalyzeAsync(Scan()));
    }

    [Fact] public async Task 坏输出返回空建议不抛()
    {
        _client.Reply = "not json";
        var a = await _svc.AnalyzeAsync(Scan());
        Assert.Empty(a.Cleanable);
        Assert.Empty(a.Migratable);
    }

    [Fact] public async Task 提示词包含扫描数据与可用盘符()
    {
        await _svc.AnalyzeAsync(Scan());
        Assert.Contains("Videos", _client.LastPrompt);
        Assert.Contains("D:\\", _client.LastPrompt);
    }
}
```
(LocalizationService 构造按 M2 测试实际约定,i18n 目录复制到输出 — 实施者照 AnalysisServiceTests 先例。)

- [ ] **Step 2: 运行确认失败** → 编译失败

- [ ] **Step 3: 实现**

`Models/DiskAdvice.cs` 按头部。`DiskAdviceService`:
- 内嵌 PromptTemplate 常量(磁盘专用:输入 FolderSizeInfo JSON + 可用固定盘列表;输出格式 cleanable[{path,reason,estMB}]/migratable[{path,reason,target}];规则:只建议清单内路径、系统路径不出现、reason 用{语言})
- user prompt:扫描 JSON(path/sizeMB/fileCount/category)+ 可用盘符 + `请只输出 JSON`
- 解析:容错截取首个{到末个}(与 AnalysisResultParser 同款手法,独立私有方法),逐条 try/catch
- 过滤:cleanable/migratable 均须 ①在扫描候选集中(OrdinalIgnoreCase)②!SystemPathGuard.IsProtected ③migratable 的 target 须在 availableFixedDrives 中
- estMB→EstBytes;Token 记 `AnalysisTrigger.Manual`(AnalysisTrigger 不加新值,token 表 Model 字段标 "disk" 前缀?不 — 记 profile.Model 即可,Trigger=Manual)
- AdviceCompleted 事件

- [ ] **Step 4: 运行确认通过** → 5 passed;全量 143 passed

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "磁盘建议服务(FR-12.2):LLM分析扫描结果、系统路径+候选集+盘符三重过滤、Token记录(TDD)"
```

---

### Task 11: RecycleBinDeleteService + MigrationService(TDD)

**Files:**
- Create: `src/AiMemoryManager/Services/RecycleBinDeleteService.cs`, `src/AiMemoryManager/Models/MigrationLogEntry.cs`, `src/AiMemoryManager/Services/MigrationService.cs`
- Test: `tests/AiMemoryManager.Tests/RecycleBinDeleteServiceTests.cs`, `tests/AiMemoryManager.Tests/MigrationServiceTests.cs`

**Interfaces:**
- Produces: 头部两服务。MigrationService 的 robocopy/mklink 通过注入 `Func<string[], int> runner`(默认实现跑进程)便于测试。

- [ ] **Step 1: 写失败测试**

`RecycleBinDeleteServiceTests.cs`:
```csharp
using AiMemoryManager.Services;

namespace AiMemoryManager.Tests;

public class RecycleBinDeleteServiceTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "amm-test-" + Guid.NewGuid().ToString("N"));
    public RecycleBinDeleteServiceTests() => Directory.CreateDirectory(_dir);
    public void Dispose() { if (Directory.Exists(_dir)) Directory.Delete(_dir, true); }

    [Fact] public void 删除目录后原路径不存在()
    {
        var target = Path.Combine(_dir, "todel");
        Directory.CreateDirectory(target);
        File.WriteAllText(Path.Combine(target, "f.txt"), "x");
        Assert.True(new RecycleBinDeleteService().DeleteDirectoryToRecycleBin(target));
        Assert.False(Directory.Exists(target));
    }

    [Fact] public void 不存在路径返回false不抛()
    {
        Assert.False(new RecycleBinDeleteService().DeleteDirectoryToRecycleBin(Path.Combine(_dir, "nope")));
    }
}
```
(回收站真实性靠人工清单验证 — 单元测试只验"路径消失且不永久报错";Windows 上 Microsoft.VisualBasic 删除小目录必走回收站。)

`MigrationServiceTests.cs`:
```csharp
using AiMemoryManager.Models;
using AiMemoryManager.Services;
using AiMemoryManager.Tests.Fakes;

namespace AiMemoryManager.Tests;

public class MigrationServiceTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "amm-test-" + Guid.NewGuid().ToString("N"));
    private readonly string _src, _dst, _log;
    private readonly List<string[]> _runs = new();
    private readonly FakeNativeMemoryApi _native = new();
    private MigrationService Svc() => new(_native, _log, runner: args => { _runs.Add(args); return RunFake(args); });

    public MigrationServiceTests()
    {
        Directory.CreateDirectory(_dir);
        _src = Path.Combine(_dir, "Games");
        _dst = Path.Combine(_dir, "D");          // 模拟目标盘根
        _log = Path.Combine(_dir, "m.json");
        Directory.CreateDirectory(_src);
        Directory.CreateDirectory(_dst);
        File.WriteAllText(Path.Combine(_src, "a.dat"), "data");
    }
    public void Dispose() => Directory.Delete(_dir, true);

    private int RunFake(string[] args)
    {
        // 模拟 robocopy /E src dst:复制文件;mklink:创建标记文件充当 junction
        if (args[0] == "robocopy")
        {
            var (s, d) = (args[^2], args[^1]);
            Directory.CreateDirectory(d);
            foreach (var f in Directory.GetFiles(s)) File.Copy(f, Path.Combine(d, Path.GetFileName(f)), true);
            return 1;   // robocopy 退出码 <8 为成功
        }
        if (args[0] == "mklink") { File.WriteAllText(args[^2] + ".junction", args[^1]); return 0; }
        return 0;
    }

    [Fact] public void 占用检测_路径前缀匹配()
    {
        _native.Processes.Add(new ProcessSnapshot(1, "game", Path.Combine(_src, "game.exe"), 100L << 20, true));
        _native.Processes.Add(new ProcessSnapshot(2, "other", @"C:\other\x.exe", 100L << 20, true));
        var blocking = Svc().GetBlockingProcesses(_src);
        Assert.Equal(new[] { "game" }, blocking);
    }

    [Fact] public async Task 迁移流程_复制校验删源建Junction记日志()
    {
        var entry = await Svc().MigrateAsync(_src, _dst);
        Assert.True(File.Exists(Path.Combine(_dst, "Games", "a.dat")));   // 已复制
        Assert.False(Directory.Exists(_src));                              // 源已删
        Assert.Equal(_src, entry.Source);
        Assert.False(entry.Reverted);
        Assert.Single(Svc().Log);
    }

    [Fact] public async Task 复制失败抛异常且现场保留()
    {
        var svc = new MigrationService(_native, _log, runner: _ => 8);   // robocopy 退出码>=8 失败
        await Assert.ThrowsAsync<InvalidOperationException>(() => svc.MigrateAsync(_src, _dst));
        Assert.True(Directory.Exists(_src));
        Assert.True(File.Exists(Path.Combine(_src, "a.dat")));
    }

    [Fact] public async Task 回退_删Junction移回源更新日志()
    {
        var svc = Svc();
        var entry = await svc.MigrateAsync(_src, _dst);
        Assert.True(await svc.RevertAsync(entry));
        Assert.True(File.Exists(Path.Combine(_src, "a.dat")));   // 移回
        Assert.True(svc.Log[0].Reverted);
    }
}
```

- [ ] **Step 2: 运行确认失败** → 编译失败

- [ ] **Step 3: 实现**

`RecycleBinDeleteService` 用 `Microsoft.VisualBasic.FileIO.FileSystem`:
```csharp
using Microsoft.VisualBasic.FileIO;

namespace AiMemoryManager.Services;

public class RecycleBinDeleteService
{
    public bool DeleteDirectoryToRecycleBin(string path)
    {
        try
        {
            if (!Directory.Exists(path)) return false;
            FileSystem.DeleteDirectory(path, UIOption.OnlyErrorDialogs, RecycleOption.SendToRecycleBin);
            return true;
        }
        catch { return false; }
    }

    public bool DeleteFileToRecycleBin(string path)
    {
        try
        {
            if (!File.Exists(path)) return false;
            FileSystem.DeleteFile(path, UIOption.OnlyErrorDialogs, RecycleOption.SendToRecycleBin);
            return true;
        }
        catch { return false; }
    }
}
```

`MigrationService` 要点(完整实现由实施者按测试行为写):
- ctor 注入 `Func<string[], int>? runner = null`;默认 runner 用 Process 跑 `robocopy`(参数 `{ "/E", "/NFL", "/NDL", "/NJH", "/NJS", "/NP", src, dst }`,退出码 <8 成功)与 `cmd /c mklink /J "link" "target"`(退出码 0)
- MigrateAsync:① GetBlockingProcesses 非空→抛 InvalidOperationException(带进程名)② target = Path.Combine(targetRoot, 源文件夹名),已存在→抛 ③ robocopy 复制 ④ 文件数校验(源 vs 目标递归计数,不等→抛,保留现场)⑤ 永久删除源(Directory.Delete recursive;**删前 UI 已明确警告**,代码注释标明这是有意永久删除,回收站装不下大迁移)⑥ mklink /J 在原位置建 junction ⑦ 记日志(原子写,容量 50,新在前)
- RevertAsync:删 junction(若是真 junction 用 `Directory.Delete(junction, false)` 不递归)→ robocopy /E 移回 → 删 target → 日志 Reverted=true → true;任一步失败 false
- 日志加载损坏回退空

注意:测试里 mklink 的"junction"是标记文件,RevertAsync 的"删 junction"逻辑要兼容 File/Directory 两种存在形式(实施者注释说明测试替身)。

- [ ] **Step 4: 运行确认通过** → 6 passed;全量 149 passed

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "回收站删除与迁移(FR-12.3/12.4):RecycleBin删除、robocopy复制校验、Junction、日志回退、占用检测(TDD)"
```

---

### Task 12: C 盘瘦身页 UI(FR-12 集成)

**Files:**
- Create: `src/AiMemoryManager/ViewModels/CSlimViewModel.cs`, `src/AiMemoryManager/Views/CSlimPage.xaml(.cs)`
- Modify: `src/AiMemoryManager/Services/Locator.cs`, `src/AiMemoryManager/MainWindow.xaml`(导航项,插"Token 统计"之前), i18n 双文件

**Interfaces:**
- Consumes: `Locator.DiskScan`(DiskScanService)、`Locator.DiskAdvice`(DiskAdviceService,availableFixedDrives 由 `DriveInfo.GetDrives()` 过滤 Fixed+Ready+非系统盘)、`Locator.RecycleBin`、`Locator.Migration`、`DiskCandidateProvider`

- [ ] **Step 1: Locator 接线 + 导航 + i18n key 批量**

Locator 新增 `DiskScan/DiskAdvice/RecycleBin/Migration` 四属性并 Init;导航项 `Nav.CSlim`(C 盘瘦身/Drive C Slimmer),图标用有效 SymbolRegular(如 `HardDrive24`,构建验证);i18n key(双语):
```
Nav.CSlim, Slim.Scan(扫描占用), Slim.Scanning(扫描中…), Slim.Size(大小), Slim.Category(类别),
Slim.Analyze(Llm分析), Slim.NoProfile(请先配置大模型), Slim.Advice(分析建议),
Slim.Cleanable(可清理项), Slim.Migratable(可迁移项), Slim.Reason(理由), Slim.EstFree(预估释放),
Slim.TargetDrive(目标盘), Slim.DeleteSelected(删除选中(入回收站)), Slim.MigrateSelected(迁移选中),
Slim.ConfirmDelete(将把 {0} 项移入回收站,可从回收站恢复。继续?),
Slim.ConfirmMigrate(将迁移 {0} 项到 {1},原位置保留链接,源文件经复制校验后永久删除(不进回收站)。继续?),
Slim.Blocked(以下项被进程占用,请先关闭:{0}),
Slim.DeleteDone(已移入回收站 {0} 项), Slim.MigrateDone(已迁移 {0} 项),
Slim.MigrateLog(迁移记录), Slim.Revert(回退), Slim.Reverted(已回退), Slim.RevertDone(已回退 {0}),
Slim.Protected(系统保护路径,不可操作), Slim.Empty(暂无数据,请先扫描)
```

- [ ] **Step 2: CSlimViewModel**

- `[RelayCommand] ScanAsync`:DiskCandidateProvider.GetCandidates → DiskScan.ScanAsync → `ObservableCollection<FolderSizeInfo> ScanResults`(按 SizeBytes 降序,0 项过滤)→ DataGrid 展示
- `[RelayCommand] AnalyzeAsync`:无档案→状态 `Slim.NoProfile`;有则 DiskAdvice.AnalyzeAsync(ScanResults)→ Cleanable/Migratable 两 `ObservableCollection`,行带 `bool IsSelected`
- `[RelayCommand] DeleteSelectedAsync`:勾选项 → 逐项 SystemPathGuard.IsProtected 重查(执行端不信 UI)→ ContentDialog 确认(`Slim.ConfirmDelete`)→ RecycleBin.DeleteDirectoryToRecycleBin → 结果文本 → 重扫该项大小(或移除行)
- `[RelayCommand] MigrateSelectedAsync`:勾选项 → 逐项 GetBlockingProcesses → 有占用弹 `Slim.Blocked` 中止 → ContentDialog 确认(`Slim.ConfirmMigrate`,文案含永久删除警告)→ Migration.MigrateAsync → 结果 → 刷新迁移记录
- `[RelayCommand] RevertAsync(MigrationLogEntry entry)` → Migration.RevertAsync → 结果文本
- 迁移记录区:Locator.Migration.Log 绑定(未 Reverted 的行显示"回退"按钮)
- 全部 async 命令 try/catch;WPF-UI ContentDialog 用法照 Task 7 对话框先例

- [ ] **Step 3: CSlimPage XAML**

四个 Card:① 扫描结果(按钮+DataGrid:路径/大小 MB/类别/文件数)② LLM 建议-可清理(勾选 DataGrid:路径/理由/预估释放,底部"删除选中")③ 可迁移(勾选 DataGrid:路径/理由/目标盘,底部"迁移选中")④ 迁移记录(时间/源/目标/状态/回退按钮)。照 M1/M2 页模式。

- [ ] **Step 4: 构建+测试+冒烟** → 全量 149 passed 不变;10s 冒烟(导航可达)

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "C盘瘦身页(FR-12):扫描/LLM建议/勾选删除入回收站/迁移+回退全流程UI"
```

---

### Task 13: 技术债 — 原子写统一 + ProfileId + SkippableFact

**Files:**
- Modify: `src/AiMemoryManager/Services/SettingsService.cs`(Persist 换 AtomicFile)、`LlmProfileService.cs`、`PromptTemplateService.cs`(同换), `src/AiMemoryManager/Services/TokenStatsService.cs`(TokenUsageRecord 加 ProfileId), `src/AiMemoryManager/Services/AnalysisService.cs`(Record 带 profile.Id), `src/AiMemoryManager/Services/DiskAdviceService.cs`(同), `src/AiMemoryManager/ViewModels/TokenStatsViewModel.cs`(费用按 Id 匹配,名称回退), `tests/AiMemoryManager.Tests/LlmIntegrationTests.cs`(换 SkippableFact)
- Modify: csproj 加 `Xunit.SkippableFact` 包
- Test: `tests/AiMemoryManager.Tests/TokenStatsServiceTests.cs`(加 ProfileId 双写兼容例)

- [ ] **Step 1: 原子写替换** — 三处服务的 File.WriteAllText 换 AtomicFile.WriteAllText,跑既有测试确认全绿(行为不变,无需新测试)

- [ ] **Step 2: ProfileId 双写兼容(TDD)**

先写测试:
```csharp
[Fact] public void 旧记录无ProfileId兼容读取()
{
    // 手写一行无 ProfileId 字段的 jsonl,LoadAll 不丢、ProfileId 为 null
}
[Fact] public void 新记录带ProfileId()
{
    // Record 带 ProfileId → LoadAll 读回
}
```
`TokenUsageRecord` 加 `string? ProfileId`(放最后,带默认值 — positional record 加参数须给默认值保兼容,实施者处理:System.Text.Json 对缺失字段用默认值);AnalysisService/DiskAdviceService Record 调用带 `profile.Id`;TokenStatsViewModel 费用/分组优先按 ProfileId 匹配档案,为 null 回退按名(注释说明旧数据兼容)。

- [ ] **Step 3: SkippableFact**

`dotnet add tests/AiMemoryManager.Tests package Xunit.SkippableFact`;LlmIntegrationTests 两个 `[Fact]` 换 `[SkippableFact]`,首行 `Skip.If(string.IsNullOrEmpty(Key), "未配置 AMM_TEST_LLM_KEY");`,删早退 return。无密钥跑全量:96 passed + 2 skipped。

- [ ] **Step 4: 全量回归** → 有密钥 151/151;无密钥 149 passed + 2 skipped

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "技术债:JSON存储统一原子写、Token记录带ProfileId(旧数据兼容)、集成测试显式Skip"
```

---

### Task 14: 收尾 — 回归 + 真实端点冒烟 + 人工清单

**Files:**
- Create: `docs/manual-verification-m3.md`

- [ ] **Step 1: 全量回归**

build 0 error;全量测试(带密钥跑集成):全绿;15s 启动冒烟(Debug 无异常);`git grep -E "sk-[A-Za-z0-9]{20,}"` 零命中。

- [ ] **Step 2: docs/manual-verification-m3.md**

- L3:进程页勾选 chrome(先开个带未保存的记事本验证高风险标记)→ 确认对话框 → 执行 → 内存释放 → 后悔药恢复(记事本重开)
- 防误杀:白名单页(或设置)把某进程加防误杀 → 该进程勾选框禁用;大模型 terminate 建议被过滤
- 分析页 terminate 建议:可执行且走确认流
- 热键:Ctrl+Shift+M 触发清理;设置页改键生效
- 自启:开开关 → 注册表 Run 键出现 → 重启资源管理器或重登验证(可选);关开关键消失
- 通知:关通知开关 → 清理完成不弹;开 → 弹
- C 盘瘦身:扫描 → 大小排序合理;LLM 分析(DeepSeek)→ 建议出现;勾 Temp 删除 → 回收站可见;迁移个小文件夹到 D 盘 → 原位 Junction 可用(资源管理器打开无感)→ 回退成功;占用检测:迁移前打开该文件夹内程序验证被拦
- 历史:各类清理(L1/L2/L3)在仪表盘历史卡出现,超 100 条截断
- 密钥:llm-profiles.json 无明文

- [ ] **Step 3: Commit + push**

```bash
git add -A
git commit -m "M3收尾:全量回归、人工验证清单"
git push
```

---

## Self-Review 记录

- **Spec 覆盖**:FR-2.3→T4/T7;FR-2.5→T1(CleanHistoryService)+T5(CleanService 接线)+T7(仪表盘历史卡);FR-2.6→T3/T7;FR-2.7→T4/T7;FR-7.2→T2(服务+分析过滤)/T7(进程页 CanKill+白名单页第二列表);FR-8.3→T5;FR-8.4→T5;FR-8.5→T6;FR-12.1→T9;FR-12.2→T10;FR-12.3→T11/T12;FR-12.4→T11/T12;FR-12.5→T9(SystemPathGuard)+T11(占用)+T12(执行端重查)。FR-8.3 的 MSIX StartupTask 声明属 M4 打包,本计划做运行时开关+注册表回退(计划级声明)。
- **已知妥协**:回收站真实性只入人工清单(单元不可测);迁移源删除为永久(对话框明确警告,回收站不适合大迁移);后悔药命令行依赖 WMI(失败回退仅路径);DiskAdvice 提示词不进模板服务(单一用途,YAGNI)。
- **类型一致性**:CleanHistoryEntry 复用 CleanLevel/CleanTrigger(L3 与 Analysis 已在 T1 扩展);L3 历史由调用方 VM 记(进程页 Manual、分析页 Analysis),CleanService 历史由 Locator 接线 — 无双记;TerminateConfirmDialog 为进程页(T7)与分析页(T8)共用产物;FakeNativeMemoryApi 的窗口标题/终止行为测试钩子以 T1 实际产物为准。
