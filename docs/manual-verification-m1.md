# M1 人工验证清单

自动化已覆盖:Release 构建(0 警告 0 错误)、当前全量 171 个测试(169 通过、2 跳过)、启动冒烟验证。
以下项涉及真实桌面交互 / UAC / 视觉效果,无法无头验证,请人工走查。

## 1. L2 深度清理首用流程(重点)
- [x] 主程序输出目录存在 `AiMemoryManager.ElevatedHelper.exe`(2026-08-15 复核确认)
- [~] 仪表盘点「深度清理」→ 弹出 UAC 授权框 → 授权后清理成功,显示「上次清理释放 X MB」
  (2026-08-16:本机 EnableLUA=0 完全禁用 UAC,注册计划任务静默完成、清理成功释放 231MB(清理历史有 L2 记录);UAC 弹窗本身留待干净设备验证)
- [x] 再次点「深度清理」→ **不再弹 UAC**(计划任务已注册,直接执行)(2026-08-16 通过,本机 UAC 禁用下直接执行)
- [~] UAC 弹窗点「否」→ 不崩溃,仪表盘显示「深度清理需要一次管理员授权,授权后不再询问(...)」
  (本机无 UAC 弹窗可点,留待干净设备验证)
- [x] 规则页开启「自动清理包含深度清理」并触发自动清理 → 同样只首次要授权(2026-08-20 通过;本机 UAC 禁用下全程无交互自动执行)

## 2. 托盘
- [x] 托盘图标为动态百分比圆环,内存占用升高时颜色 绿→橙→红 变化(2026-08-15 通过)
- [x] 悬停提示显示「内存占用 X%」(2026-08-15 通过)
- [x] 右键菜单:一键清理(完成后弹通知「清理完成,释放 X MB」)/ 打开主界面 / 退出(2026-08-15 通过)
- [x] 点窗口关闭按钮 → 最小化到托盘而不是退出;托盘「退出」才真正退出(2026-08-15 通过)
- [x] 启动第二个实例 → 立即退出(单实例)(2026-08-20 通过:第二实例启动后自动退出,仅原进程保留)

## 3. 仪表盘
- [x] 圆环百分比、已用/可用/总计实时刷新(2026-08-20 通过)
- [x] 底部曲线随时间滚动(2026-08-20 通过)
- [x] 「一键清理」按钮按下后释放内存并显示释放量(2026-08-20 通过)

## 4. 规则
- [x] 阈值规则:把阈值临时调到远低于当前占用(如 1%)、持续 10 秒 → 自动触发清理(验证后改回)(2026-08-20 通过)
- [x] 定时规则:设 1 分钟 → 到点自动清理(2026-08-20 通过)
- [x] 全屏抑制:勾选后进入全屏应用(如视频全屏)→ 不触发自动清理(2026-08-20 UI 自动化通过:WS_POPUP 全屏窗口使 SHQueryUserNotificationState=2,全屏 75s 内零触发,关窗后阈值规则 90s 内恢复触发;注:阈值下限被 Normalize 钳制在 40%,测试用内存压力把占用推到 45%)
- [x] 规则修改即改即存,重启应用后保留(2026-08-20 通过)

## 5. 进程页 / 白名单
- [x] 进程列表按内存排序、刷新按钮可用(2026-08-20 自动化通过;发现并修复:手动刷新在实时刷新进行中会被静默丢弃且不恢复排序,已改为排队执行+强制重排,含 4 个回归单测)
- [x] 右键「加入白名单」→ 白名单页出现;移除、导入、导出 .txt 均正常(2026-08-20 UI 自动化端到端通过,settings.json 佐证)
- [x] 白名单内进程不会被清理影响(可在清理前后对比其工作集)(逻辑层有单测 `L1_跳过白名单_系统关键_前台进程` 覆盖)

## 6. 外观与语言
- [ ] 主题跟随系统(切换 Windows 深/浅色,窗口 Mica 背景随之变化) — 需人工目测
- [x] 设置页切换 中文/English/跟随系统 → 全部界面文字即时切换(2026-08-20 UI 自动化通过:auto→en→zh-CN,导航文本即时变更并持久化)
- [x] 「界面动效」开关状态重启后保留(2026-08-20 UI 自动化通过:切换→杀进程重启→状态与 settings.json 一致)

## 7. 稳定性
- [ ] 托盘清理失败时(极端情况)弹错误通知而非崩溃 — 需人为制造失败场景,留待干净设备与 UAC 项一起验证
- [x] 断网/长时间运行(>10 分钟)无内存句柄泄漏迹象(2026-08-20:tools/gdi-monitor.ps1 连续采样 44 分钟,GDI 恒定 25、USER 稳定 33-36、句柄 684→580 下降、线程 13-19 平稳,artifacts/gdi-monitor.csv)
