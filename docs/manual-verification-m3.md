# M3 人工验证清单

自动化已覆盖:Release 构建(0 警告 0 错误)、当前全量 188 个测试(186 通过、2 个真实 LLM 端点测试因未配置 `AMM_TEST_LLM_KEY` 跳过;配置后已实测通过)；启动冒烟、密钥扫描零命中、i18n 键对齐(zh-CN / en 各 260 键,无缺失)。
以下项涉及真实桌面交互 / 真实大模型服务 / 注册表 / 回收站与 Junction。**大部分已由 `tools/ui-smoke` FlaUI 自动化实测通过**(真实 DeepSeek 调用、真实终止/清理),剩余少数项请人工走查。

## 1. L3 进程终止确认流(重点)
- [x] 先打开记事本,输入任意内容但**不保存**(制造未保存状态) —— 自动化:专用临时文件 uismoke-l3.txt + 键入内容,标题出现 `*` 标记
- [x] 进程页勾选该记事本进程 → 该行出现「高风险」标记(未保存工作提示) —— 自动化实测:确认对话框中带高风险标记 ✓
- [x] 再勾选其它进程 → 点「结束所选」→ 弹出确认对话框,列出所选进程与风险提示 —— 自动化:notepad + uismokel3 双进程均列出 ✓
- [x] 对话框中确认执行 → 进程被终止,仪表盘可见内存释放 —— 自动化:确认后两进程均退出 ✓
- [ ] 「后悔药」恢复:在终止结果/历史中点恢复 → 记事本被重新打开(依赖 WMI 记录命令行;失败时按路径回退重开) —— ⚠️ **留人工**:恢复逻辑(KillLogService.Restart,路径回退)有单测覆盖,但「恢复」按钮在滚动页底部 ListView 内,本机远程会话下 UIA Invoke 对该按钮静默无效,自动化点不到。请人工点一次验证。

## 2. 防误杀(白名单)
- [x] 白名单页把某进程(如 notepad)加入防误杀 → 进程页该进程的勾选框变为禁用 —— 自动化实测 ✓
- [x] 智能分析中若大模型给出该进程的 terminate 建议 → 该建议被过滤,不出现在建议卡片中 —— 自动化:模板固定输出 terminate notepad(真实 DeepSeek 调用),建议卡被过滤 ✓

## 3. 分析页 terminate 建议执行
- [x] 「智能分析」页拿到 terminate 建议 → 点执行 → **同样走 L3 确认对话框**(与进程页共用确认流),确认后才真正终止 —— 自动化实测:同一确认对话框 → 确认后 uismokel3 终止 → 历史写入 Level=L3/Trigger=分析 ✓

## 4. 热键
- [x] 按默认热键 **Ctrl+Shift+M** → 触发清理,有完成反馈 —— 自动化:热键触发后历史新增 Trigger=手动 记录 + 系统 toast 出现 ✓
- [x] 设置页把热键改为其它组合(如 Ctrl+Alt+K)→ 新键生效、旧键不再触发;重启应用后新键仍生效 —— 自动化:改为 Ctrl+Alt+K 重启后旧键无响应、新键触发 ✓
- [x] 占用降级:先用其它程序注册占住该组合键(或改成一个已被系统占用的组合)→ 应用不崩溃,静默降级(仅不注册热键),界面/日志有体现 —— 自动化实测 ✓(ui-smoke `hotkeydegrade`)。注:被 RegisterHotKey 占住的组合由 OS 直接投给占用方,前台应用焦点控件收不到该键,故「占用时往设置框敲该组合」在现实世界不可达;自动化验证的是启动期降级路径:占住 Ctrl+Shift+M 后启动应用 → 不崩溃、热键不生效(无清理记录)、设置页出现「热键注册失败」提示、一键清理等其它功能正常;解除占用重启后热键恢复、设置未被改动。配套改动:启动注册失败此前仅写 Debug 日志,现在设置页会显示降级提示(SettingsViewModel 构造时读取 Hotkey.IsRegistered)

## 5. 开机自启
- [x] 未打包 Debug/发布目录运行时：设置页打开「开机自启」→ 注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 下出现 `AiMemoryManager` 值。 —— 自动化:开关切换后注册表值出现/消失均实测 ✓
- [ ] MSIX 打包运行时：设置页打开「开机自启」→ `StartupTask` 状态变为 Enabled，关闭后变为 Disabled（不写入旧 Run 键）。 —— 需打包安装环境,留人工
- [ ] (可选)重启资源管理器或注销重登 → 应用随系统启动。
- 注意：必须用编译产物的 exe 或已安装 MSIX 验证自启，不能用 `dotnet run` —— 后者 `Environment.ProcessPath` 指向 `dotnet.exe`。

## 6. 通知开关
- [x] 关闭「清理完成通知」→ 执行一次清理 → **不弹**系统通知 —— 自动化:关开关后清理完成 90s 无 toast ✓
- [x] 打开开关 → 再执行一次清理 → 弹出「清理完成,释放 X MB」通知 —— 自动化:toast(Windows.UI.Core.CoreWindow)捕获 ✓

## 7. C 盘瘦身(重点)
- [x] 打开 C 盘瘦身页 → 扫描 → 结果按大小排序合理(大项在前) —— 自动化实测(ui-smoke `cslim`):扫描 308s 完成共 100 项,可见行大小严格降序(前三 90GB/34GB/14GB) ✓
- [x] 点 LLM 分析(DeepSeek 档案)→ 出现瘦身建议(迁移 / 删除 / 保留)及理由 —— 自动化实测:来源=Llm(真实 DeepSeek 调用),产出 8 条可清理建议且均带理由与预估释放(首条 Temp\Docker 3.41GB) ✓
- [ ] 勾选 Temp 类项目执行删除 → 文件**进入回收站**,在回收站可见且可还原 —— 留人工:自动化删真实用户文件风险不可控(可能误伤运行中程序的临时文件),请人工勾一项小 Temp 验证
- [ ] 选一个小文件夹迁移到 D 盘 → 迁移后原位置变为 Junction;用资源管理器打开原路径**无感**(直接看到内容) —— 留人工:真实文件搬迁 + Junction 创建,自动化风险高
- [ ] 执行回退 → 文件搬回原位,Junction 移除 —— 留人工(随上一条一起验证)
- [ ] 占用检测:迁移前先打开该文件夹内的程序/文件使其被占用 → 迁移被拦截并给出占用提示 —— 留人工(随迁移一起验证:迁移前打开目标文件夹内任一文件)
- [ ] 注意:迁移源目录的删除为**永久删除**(对话框有明确警告),仅删除操作走回收站 —— 属预期设计,人工验证迁移时顺带确认对话框文案

## 8. 历史
- [x] 分别执行 L1(一键清理)、L2(深度清理)、L3(进程终止)→ 仪表盘历史卡均出现对应记录(触发方式:手动 / 分析 等) —— 自动化:L1/L2/L3 三条均在历史卡出现 ✓(L2 深度清理在本机 EnableLUA=0 环境无 UAC 直接执行)
- [x] 历史超过 100 条后自动截断,只保留最近 100 条 —— 自动化:灌入 100+ 条后验证截断且最新记录保留 ✓

## 9. 密钥与真实端点(需自备 API Key)
- [x] 用文本编辑器打开数据目录下 `llm-profiles.json` → API Key 为 DPAPI 密文,**无明文** —— M2 第 9 节已自动化验证(密文无明文子串,坏密钥对照实验证明真实性)
- [x] 设置环境变量 `AMM_TEST_LLM_KEY` 为自己的 DeepSeek 密钥后,运行 `dotnet test` → 2 个真实端点集成测试由跳过变为通过 —— 已实测:配置后 2 个集成测试通过 ✓
